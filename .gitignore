# 資料與權重不進版控
data/
*.mp4
*.pt
*.onnx
*.engine

# 結果輸出:文字報告(.md/.csv)可進版控,但 EPFL 衍生影像一律排除
# (EPFL 為 CC BY-NC + 畫面含人臉隱私,不得進公開 repo)
results/**/*.png
results/**/*.jpg
results/**/*.jpeg
results/*.png
results/frames/
# zone 標註預覽圖 / HTML 含 EPFL 衍生畫面(人臉隱私),不入庫
*.preview.png
results/**/*.html

# 交付給標註幫手的整包(含內嵌 EPFL 衍生畫面,CC-BY-NC,不入庫)
標註交付包_*/

# 訓練權重(大檔;EPFL 丟棄驗證模型),不入庫
*.pth
model_result/

# ── 2026-09-04:放行**我們自己訓練**的權重,讓遠端那台可以直接訓練/推論 ──
# 用 Git LFS 存(見 .gitattributes)—— 兩個 checkpoint 都超過 GitHub 的 100 MB
# 硬限制,不走 LFS 的話 push 會直接被拒絕。
!model_result/
!model_result/nano/
!model_result/nano/*.pth
!model_result/medium/
!model_result/medium/*.pth
# M4 追蹤輸出(標註幀/軌跡紀錄)
results/m4_track/
# M5 Re-ID 驗證輸出(EPFL 衍生 crops / 結果 json,研究限定)
results/m5_reid/
reid_epfl_results.json
reid_eval_results.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# 假廚房動畫的逐幀圖(數百張,可由 scripts/render_fake_kitchen.py 重新產生)
results/m5_sim/kitchen/

# 全長跑的逐幀輸出(可由 scripts/m5_track_video.py 重新產生)
results/m5_full/frames/
results/m5_full/tracks.csv

# Re-ID 訓練產物(權重不入庫;CHIRLA 為 CC-BY-4.0 但模型檔體積大)
model_result/reid/
model_result/reid/external/
model_result/reid/osnet_reid.pth
chirla_index.json
# 匯出的 embedding。⚠ 2026-09-03 差點誤入庫 —— 16 個 h5 共數十 MB,
# 而且它是 checkpoint + chirla_index.json 的衍生物,重跑 export 就有。
# ⚠⚠ 以下**研究限定**的權重一律不入庫。本 repo 是 **公開的**,
#    把它們推上去等於**公開散布**研究限定權利物,而本案是要出貨的產學合作。
#    授權會傳染 —— 詳見 docs/授權盤點.md 與記憶中的 licensing-constraints。
#      · model_result/reid/external/*      Market-1501 訓練(FastReID market_bot_R50)
#      · model_result/reid/osnet_reid.pth  OSNet Re-ID 權重(原始碼自己標「研究限定,僅驗證」)
#    要在另一台用它們,請**直接複製檔案**,不要走 git。
embeddings/

# CHIRLA M4/M5 的 smoke 跑輸出(只用來量 ms/迴圈與驗管線,拋棄式)
results/chirla_smoke/

# M4/M5 網格的逐幀框(32.5 MB,佔輸出的 83%)。
# 重跑 run_m4m5_chirla_grid.py 可重新產生,而 run_meta.json 已完整記錄跑了什麼。
# ⚠ 預先登記 §8 第 6 條要的「原始配對結果」已存進 eval_m4m5_report.json 的
#   per_run.track_match,換 IoU 門檻重算不需要這份。
results/chirla_m4m5/**/tracks.csv
