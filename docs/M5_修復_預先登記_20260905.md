# 預先登記:F1 margin test 與 F2 同鏡頭互斥(2026-09-05)

> **本文件在 CHIRLA 評估集上跑任何一格之前定稿並提交 git。**
> 跑完只追加 §8 之後,§1–§7 不得修改。沿用 `docs/CHIRLA_M4M5驗證_預先登記_20260903.md` 的體例。

---

## 1. 為什麼做這件事

2026-09-04 的 CHIRLA 2×2 網格,四格全部 C 級:

| | 驗收門檻 | 實測 |
|---|---|---|
| 誤併率 | **≤ 1%** | **58.84 ~ 67.23%** |
| 碎裂率 | ≤ 5% | 22.39 ~ 31.33% |

全部觸發預先登記 §6 先寫死的失敗條件(誤併 > 30%),定性是
**「架構層問題,不是調參可解」**。§6 也先寫死了兩個處置,本輪執行其中的下游部分。

### 診斷:病灶已定位

**(a) `log K` 稀釋在數學上不可能拒絕**(`identity_st.py:151`)

```
5.0 − log(k) + app_llr < 1.609  →  k > 29.7
```

而 CHIRLA 單台鏡頭同時最多 **9** 個身份。**無論畫面裡有多少人,
重疊路徑永遠至少產生一個通過門檻的候選。** 指紋:重疊路徑
`p_break = 2.71%`(幾乎從不拒絕)但 `p_false_merge = 80.80%`。

**(b) 過門檻後唯一的排序依據是外觀,而外觀是雜訊**

重疊路徑 `p_correct = 16.49%`,而同時在場的其他身份中位是 **7** 人:

```
1/7 = 14.3%     1/6 = 16.7%     實測 16.49%
```

**統計上與「隨機挑一個」無法區分。**

⚠ 本輪實作時量到一個更精確的數字:用 `AppearanceLR.MEASURED["dinov2"]`
(同人 0.490 / 不同人 0.465)產生的 embedding,**同人與不同人的 LLR 差距只有
0.062 nats**,而正確決策需要 log(5) = 1.609 —— **差 26 倍**。

---

## 2. 本輪的兩個修法(定死)

| 代號 | 修法 | 位置 | 預設 |
|---|---|---|---|
| **F1** | margin test:`best − second < margin` 時**開新身份**而不是綁 | `identity_st.py` 決策點 | **關閉** |
| **F2** | 同鏡頭互斥:chef 已在本鏡頭上有 track → 不列為候選 | `identity_st.py` 候選集 | **關閉** |

### 為什麼 F1 的門檻取 log(cost_false_merge_over_break)

與 `llr_threshold` **同源**。兩者都是「誤併比碎裂貴 5 倍」換算出來的:
差距小於它,表示區分兩個假設的證據還不足以承擔誤併的成本。
本專案的成本比是 **誤併 : 碎裂 = 5 : 1**(誤併靜默不可回復,碎裂看得見),
所以「分不出來」的正確處置是**碎裂**,而現行架構一律轉成**誤併** —— 方向剛好相反。

### ⚠ F1 有一個必須保留的例外

**只有一個候選時不套用 margin。** 那不是「分不出來」,是「沒有別人可混淆」。
擋掉它會把單人場景的正確綁定全部打成碎裂 ——
EPFL 兩輪的候選分布分別是 `{0:1, 1:92}` 與 `{0:1, 1:136}`,**全部是單候選**,
沒有這個例外的話那兩輪會從「碎裂 0 次」變成「全碎」。

---

## 3. 執行網格(定死)

**基準格固定為 `coco_none`**(2026-09-04 的主基線),只改本輪的開關:

| 格 | F1 | F2 | 問的問題 |
|---|---|---|---|
| `base` | ✗ | ✗ | 回歸:必須與 2026-09-04 的 `coco_none` **逐位相同** |
| `f1` | ✓ | ✗ | margin test 單獨的效果 |
| `f2` | ✗ | ✓ | 同鏡頭互斥單獨的效果 |
| `f1f2` | ✓ | ✓ | 兩者一起 |

⚠ **必須分開量。** 第一輪的教訓是三個修法一起上,看不出誰有效。

**評估協定完全沿用**(不得更動):`scripts/run_m4m5_chirla_grid.py` 與
`eval_m4m5_chirla.py`、IoU 0.5、配對率下限 50%、評估集 7 個序列
(`seq_004/006/007/020/024/025/026`)、拓撲 `configs/camera_topology.chirla.yaml`。
**這樣新數字與 2026-09-04 的基線直接可比。**

---

## 4. 判準(先寫死)

### 4.1 主判準:加權成本

單看誤併或碎裂會誤導 —— F1 一定讓碎裂變差。依 5:1 成本比:

```
cost = 5 × p_false_merge + 1 × p_break
基線 coco_none = 5 × 0.5884 + 0.3133 = 3.255
```

| 結果 | 判定 |
|---|---|
| cost < 3.255 | 修法有效,保留 |
| cost ≥ 3.255 | **修法無效,關閉並照實記錄** —— 不得因為「誤併降了」就保留 |

### 4.2 硬性驗收(不通過就是實作錯,不是效果不好)

| # | 條件 |
|---|---|
| V1 | `base` 格與 2026-09-04 的 `coco_none` **逐位相同**(誤併/碎裂/正確率/IDF1) |
| V2 | `f2` 與 `f1f2` 的 `same_cam_conflicts` **= 0** |
| V3 | `base` 的 `same_cam_conflicts` **> 0**(否則表示這個病灶不存在,F2 是空修) |
| V4 | `f1`/`f1f2` 的 `margin_blocked` **> 0** |
| V5 | `git diff` 確認 `src/m5_sim/world.py` 未被動到 |

### 4.3 ⚠ 本輪特有的失敗條件

若 `f1` 的**碎裂率 > 90%**,判定為 **「margin test 在重疊路徑上等同於關閉該路徑」**。

處置先寫死:**不是調小 margin** —— 調小等於承認我們願意接受沒有依據的綁定。
正確的解讀是「重疊路徑在沒有幾何證據時本來就不該綁」,
處置是**進行 F4(幾何一致性)**,並在報告中把 F1 定位為
**診斷工具**(把靜默的 80% 誤併轉成看得見的碎裂)而非解方。

---

## 5. 事先寫下的預測(可被否證)

| # | 預測 | 依據 |
|---|---|---|
| **1** | `f1` 的重疊路徑誤併 **< 10%**(基線 80.80%) | 外觀只能拉開 0.062 nats,margin 要 1.609 → 幾乎所有多候選綁定都會被擋 |
| **2** | `f1` 的重疊路徑碎裂 **> 90%**(基線 2.71%),觸發 §4.3 | 同上,擋掉等於開新身份 |
| **3** | `f1` 的 **cost < 3.255**(通過主判準) | 粗估:重疊 922 決策全碎、轉場 814 不變 → cost ≈ 1.84 |
| **4** | `f2` 單獨的效果 **很小**(cost 變動 < 0.2) | 同鏡頭衝突是物理上不可能的綁定,但數量應遠少於一般誤併 |
| **5** | `f1f2` ≈ `f1`(差距 < 0.2) | 兩者作用在不同層(候選集 vs 決策),但 F1 的影響量級大得多 |

⚠ 預測 1 與 2 是**同一件事的兩面**,不是兩個獨立的預測 ——
若 1 成立而 2 不成立(誤併降了、碎裂沒漲),表示那些決策**憑空消失**了,
必須先查指標而不是慶祝。這是 2026-09-04 那次「碎裂 31.33% 但兩條路徑都 0.00%」
的同一種形狀。

---

## 6. 已知限制(報告要照抄,放在結論之前)

1. **這兩個修法都是「止血」不是「治病」。** 它們讓系統**不做沒有依據的綁定**,
   但**不會讓證據變得有依據**。真正的解方是 F4(重疊路徑的幾何一致性)。
2. **CHIRLA 是辦公室不是廚房。** 絕對值不能當廚房的預測值;
   可比的是**修法之間的相對改善**。
3. **仍然沒有相機標定**,所以 `GroundPlaneLR` 與 `VelocityLR` 在本輪依舊不生效,
   轉場路徑的 54.42% 碎裂**本輪不會改善**。
4. **加權成本 5:1 這個比值本身沒有實測來源**,與「每 2~5 分鐘跨一次鏡頭」
   同屬待校準的假設(`docs/M4_M5_階段驗證報告_20260828.md:378`)。
   主判準建立在它上面,換了比值結論可能反轉。

---

## 7. 交付物

| 檔案 | 狀態 |
|---|---|
| `src/m5_reid/spatiotemporal.py` | ✅ 已加 `margin` / `same_camera_exclusive` 設定與解析 |
| `src/m5_reid/identity_st.py` | ✅ 已加 F1 決策、F2 候選過濾、兩個診斷計數器 |
| `scripts/verify_m5_fixes.py` | ✅ 已寫,本機全過(行為驗證,不需 CHIRLA 資料) |
| 本文件 | ✅ 跑之前提交 |
| `docs/M5_修復記錄_20260905.md` | 跑完後產出,固定格式的修改前後對比 |

### 本機已完成的驗證(2026-09-05)

```
scripts/verify_m5_fixes.py   全過(關閉時決定論、F1 生效、單候選例外、F2 歸零)
scripts/verify_m5.py         [ALL PASS]
scripts/verify_m5_st.py      [ALL PASS]
scripts/verify_m4.py         [ALL PASS]
scripts/verify_reid_metrics.py [ALL PASS]
```

⚠ 本機**沒有 CHIRLA 資料**,所以「修法有沒有降誤併」必須在遠端跑。
本機只能驗證「行為是否如設計」。

---

## 8. 結果(2026-09-12)

### 8.1 執行規模與硬性驗收

49 次執行(7 格 × 7 個評估序列),協定完全沿用 2026-09-04。
本節只交代 F1/F2 相關的四格(`base` / `f1` / `f2` / `f1f2`),
F4 的三格見 `docs/M5_修復F4_預先登記_20260905.md` §8。

| # | 條件 | 結果 |
|---|---|---|
| **V1** | `base` 與 2026-09-04 的 `coco_none` 逐位相同 | **通過** |
| **V2** | `f2` / `f1f2` 的 `same_cam_conflicts` = 0 | **通過**(兩格皆 0) |
| **V3** | `base` 的 `same_cam_conflicts` > 0 | **通過**(2,363) |
| **V4** | `f1` / `f1f2` 的 `margin_blocked` > 0 | **通過**(3,373 / 3,517) |
| **V5** | `src/m5_sim/world.py` 未被動到 | **通過** |

V1 的逐項比對:

| 指標 | 2026-09-04 `coco_none` | 本輪 `base` |
|---|---|---|
| 誤併率 | 0.5884 | 0.5884 |
| 碎裂率 | 0.3133 | 0.3133 |
| 正確率 | 0.0983 | 0.0983 |
| IDF1 | 0.1214 | 0.1214 |
| 轉場數 | 2,461 | 2,461 |
| ID switch | 1,339 | 1,339 |

### 8.2 主結果

| 格 | 誤併率 | 碎裂率 | 正確率 | IDF1 | cost | 對基線 |
|---|---|---|---|---|---|---|
| `base` | 58.84% | 31.33% | **9.83%** | **0.1214** | 3.255 | — |
| `f1` | **6.22%** | 92.56% | 1.22% | 0.0202 | 1.237 | −2.018 |
| `f2` | 50.51% | 45.63% | 3.86% | 0.0349 | 2.982 | −0.273 |
| `f1f2` | **3.01%** | 96.55% | 0.45% | 0.0151 | 1.116 | −2.139 |

逐路徑:

| 格 | 路徑 | 決策數 | 碎裂率 | 誤併率 | 正確率 |
|---|---|---|---|---|---|
| `base` | 重疊 | 922 | 2.71% | 80.80% | 16.49% |
| | 轉場 | 814 | 54.42% | 44.72% | 0.86% |
| `f1` | 重疊 | 922 | **90.56%** | **7.59%** | 1.84% |
| | 轉場 | 814 | 93.98% | 6.02% | 0.00% |
| `f2` | 重疊 | 922 | 17.57% | 74.51% | 7.92% |
| | 轉場 | 814 | 63.64% | 36.12% | 0.25% |
| `f1f2` | 重疊 | 922 | 94.36% | 4.45% | 1.19% |
| | 轉場 | 814 | 97.05% | 2.95% | 0.00% |

### 8.3 §4.3 的失敗條件觸發

`f1` 的碎裂率 **92.56% > 90%**,`f1f2` 是 96.55% → **§4.3 觸發**。

照 §4.3 先寫死的判定與處置:

> 判定為「margin test 在重疊路徑上等同於關閉該路徑」。
> 處置**不是調小 margin** —— 調小等於承認我們願意接受沒有依據的綁定。
> 正確的解讀是「重疊路徑在沒有幾何證據時本來就不該綁」,
> 處置是進行 F4,並在報告中把 F1 定位為**診斷工具**而非解方。

**照辦。F1 在本專案的定位是診斷工具** —— 它把靜默的 80.80% 誤併
轉成看得見的 90.56% 碎裂。那 922 次重疊路徑的綁定裡,
有八成本來就沒有依據,F1 只是讓這件事浮上檯面。

### 8.4 ⚠ 主判準 `cost` 在本輪失效 —— 這是要記下來的方法論問題

四格的 cost 全部低於基線 3.255,照 §4.1 字面判定都是「有效,保留」。
**但不能這樣報。**

```
cost = 5 × 誤併 + 1 × 碎裂
```

一個**什麼都不綁**的系統會得到 誤併 → 0、碎裂 → 1,**cost = 1.0**。
這是這個成本函數的**下界**,而且對應的是完全無用的系統。

| 格 | cost | 距「什麼都不綁」的地板(1.0) |
|---|---|---|
| `f1f2` | 1.116 | **+0.116** |
| `f1` | 1.237 | +0.237 |
| `f2` | 2.982 | +1.982 |
| `base` | 3.255 | +2.255 |

`f1f2` 幾乎貼著地板。而真正該看的兩個量:

| 格 | 正確率 | IDF1 |
|---|---|---|
| `base` | **9.83%** | **0.1214** |
| `f1` | 1.22% | 0.0202 |
| `f2` | 3.86% | 0.0349 |
| `f1f2` | 0.45% | 0.0151 |

**三個修法都讓系統更少綁對,而不是更常綁對。** 它們把錯誤從「誤併」
搬到「碎裂」,而 5:1 的權重讓這種搬運看起來像進步。

⚠ 這正是本 repo 工作文化第 6 條的形狀:**當一個修法讓主判準大幅改善時,先懷疑判準。**
`cost` 的問題是它**沒有獎勵正確**,只懲罰兩種錯誤 ——
所以「拒絕作答」是它的最佳策略。

§6 第 4 條已經先寫了「5:1 這個比值本身沒有實測來源」,但本輪暴露的問題比比值更基本:
**成本函數缺少正確率項**。修正方向(需另立預先登記):

```
cost' = 5 × 誤併 + 1 × 碎裂 − k × 正確率        或直接用 IDF1 當主判準
```

⚠ **不得在本輪事後換判準再重評** —— 那是事後合理化。
本輪照原判準記錄,同時把判準的缺陷寫清楚,由下一輪的預先登記處理。

### 8.5 §5 五條預測的逐條交代

| # | 預測 | 結果 |
|---|---|---|
| **1** | `f1` 的重疊路徑誤併 < 10%(基線 80.80%) | **成立**(7.59%) |
| **2** | `f1` 的重疊路徑碎裂 > 90%(基線 2.71%) | **成立**(90.56%),觸發 §4.3 |
| **3** | `f1` 的 cost < 3.255 | **成立**(1.237),但見 §8.4 |
| **4** | `f2` 單獨的效果很小(cost 變動 < 0.2) | **否證**(變動 0.273) |
| **5** | `f1f2` ≈ `f1`(差距 < 0.2) | **成立**(差 0.121) |

⚠ §5 有一段先寫死的警告:「若 1 成立而 2 不成立(誤併降了、碎裂沒漲),
表示那些決策**憑空消失**了,必須先查指標」。本輪 **1 與 2 同時成立**,
重疊路徑的決策數在四格都是 922(不變),所以**沒有決策憑空消失**,指標自洽。

預測 4 被否證的幅度不大(0.273 vs 門檻 0.2),但方向明確:
F2 單獨就把誤併從 58.84% 降到 50.51%。它消除的 2,363 次同鏡頭衝突
**是物理上不可能的綁定**,所以這部分改善是乾淨的 —— 與 F1 那種
「靠拒絕作答換來的」不同。

⚠ 但 F2 也讓正確率從 9.83% 掉到 3.86%,所以連它也不是純粹的改善。

### 8.6 結論

| 修法 | 照 §4.1 字面 | 實質判定 |
|---|---|---|
| **F1** | cost 1.237,有效 | **診斷工具,不是解方**(§4.3 已寫死) |
| **F2** | cost 2.982,有效 | **唯一乾淨的改善**(消除物理不可能的綁定),但幅度有限且正確率仍下降 |
| **F1+F2** | cost 1.116,有效 | 同 F1 |

**沒有任何一個修法讓系統更常綁對。** 重疊路徑真正需要的是幾何證據(F4),
轉場路徑真正需要的是出入口位置(F5)—— 而 F5 本輪因為推導集太短而建立不起連結
(見該份 §8)。

---

*本文件於在 CHIRLA 評估集上跑任何一格之前定稿。§1–§7 自提交後未修改;§8 為執行後追加。*
