# M5 結構缺口修補 —— 模擬預先登記(2026-08-25)

> **本文件在跑任何模擬之前寫定並提交。** 目的是排除「事後調參到好看為止」的可能性:
> 掃描網格、判準、要看哪幾個數字,全部先講死。跑完之後**不修改本文件的判準**,
> 若結果不如預期就照實記錄失敗。
>
> 上一輪([M5_v3_重設計與可行性驗證_20260824.md](M5_v3_重設計與可行性驗證_20260824.md))
> 沒有做這件事,是已知的方法論缺口。

---

## 1. 要修什麼(上一輪的發現)

蒙地卡羅顯示碎裂率 24.1%,其中 **84% 來自三個架構沒有建模的東西**:

| 缺口 | 佔碎裂 | 該情境碎裂率 | 根因 |
|---|---|---|---|
| 中途逗留 | 41% | 76.5% | 逗留混合模型寫在 config 裡,但 LLR 門檻仍把長延遲擋掉 —— 模型承認逗留,門檻不承認 |
| 繞路 | 25% | 90.4% | `transit_llr` 對沒有連結的鏡頭對直接回 `NEG_INF` |
| M4 斷軌 | 18% | 89.0% | `transit_llr` 第一行 `cam_from == cam_to → 拒絕`,同鏡頭斷軌後重現必定開新 chef |

正常轉場碎裂 5.1%,剛好壓在預算邊界。

---

## 2. 三個修法(設計已定,實作前登記)

### F1 未建模路徑(繞路)

不再硬拒沒有連結的鏡頭對,改成給它一個**寬且帶先驗懲罰**的轉場模型:

```
LLR_unknown = log p(Δt | 走了未建模路徑) − log λ_bg + log P(走未建模路徑)
```

- `p(Δt | 未建模)`:對數常態,中位數 = `median_multiplier × 該場域典型轉場時間`,
  `log_sigma` 大(路徑未知 → 時間變異大)
- `log P(走未建模路徑)`:負的先驗懲罰,確保「有連結的路徑」永遠優先於「沒連結的」

**預期副作用**:任何鏡頭對都變成候選 → 候選數上升 → 誤併率上升。這是要量的代價。

### F2 逗留尾部

系統目前的 `tau_loiter_s=20`。真實廚房的停留(洗手、處理食材)常在 30~120 秒,
20 秒太短 → 尾部太薄 → 長延遲的 LLR 掉到門檻以下。

修法:把系統的逗留尾部**刻意設得比預期更重**(保守方向),因為碎裂是主導失效模式。
具體值在 §3 掃描中決定,但**方向事先講死:只往加重尾部的方向調,不往減輕調**。

### F3 同鏡頭重關聯(M4 斷軌)

新增一條路徑:若某 chef 的出口在**同一台**鏡頭、且 Δt 很小,視為 M4 斷軌而非
真的離場再回來。

```
LLR_same_cam = log p(Δt | 軌跡中斷) − log λ_bg
p(Δt | 軌跡中斷) = Exp(1/tau_break),僅在 Δt ≤ max_gap_s 內有效
```

⚠ 目前只用時間,**不用位置**。`identity_st.py` 的 `bbox` 參數仍是死參數,
若日後接上,加入「斷軌前後 bbox 接近」會顯著收緊這條路徑、降低誤併。
本輪不做,但在此登記為已知的可強化點。

---

## 3. 掃描網格(事先固定)

**系統參數**(要調的):

| 參數 | 掃描值 |
|---|---|
| `unknown_path.logprior` | −3.0, −2.0, −1.0(越負越保守) |
| `unknown_path.log_sigma` | 0.8 |
| `tau_loiter_s`(系統端) | 20(現況), 45, 90 |
| `same_camera.tau_break_s` | 2.0 |
| `same_camera.max_gap_s` | 5, 15 |

**世界參數**(不變,與上一輪相同,確保可比):
`n_chefs=4`、`p_loiter=0.15`、`tau_loiter_s=20`、`p_detour=0.05`、
`m4_fragment_rate=0.05`、`m4_miss_rate=0.02`、對數常態 median 4.0 / logσ 0.35。

**執行規格**:`--reps 20 --hours 1.5`(與上一輪相同,6000+ 次轉場)。

---

## 4. 判準(事先講死)

### 主判準

| 指標 | 門檻 | 來源 |
|---|---|---|
| 整體碎裂率 `p_break` | **≤ 5%** | 由 M6「未洗手」事件鏈 K=3 次交接、recall ≥ 0.80 反推 |
| 整體誤併率 `p_false_merge` | **≤ 1%** | 誤併=靜默漏報,比碎裂嚴 5 倍 |

### 次判準(必須同時檢查,防止「拆東牆補西牆」)

1. **正常轉場的碎裂率不得惡化**:目前 5.1%,修完不得 > 6.0%。
2. **誤併率不得因 F1 失控**:若 F1 讓誤併率超過 3%,判定 F1 失敗,改用更負的先驗
   或關閉 F1(記錄為「繞路無法在不犧牲誤併的前提下修復」)。
3. **消融健全性檢查必須仍然通過**(完整系統落在消融組與 oracle 之間)。
4. **既有 12 項回歸測試必須全過**。

### 結果分級(事先定義,跑完直接對照)

| 級別 | 條件 | 結論 |
|---|---|---|
| **A 達標** | 碎裂 ≤5% 且誤併 ≤1% | 架構可行,進入交付階段 |
| **B 部分達標** | 碎裂 ≤10% 且誤併 ≤3% | 架構方向對但不足,需要位置線索或更強外觀 |
| **C 未達標** | 其餘 | 三個修法不足以救,需重新考慮架構(如加裝走廊鏡頭把不重疊變重疊) |

**若落在 B 或 C,照實記錄,不得回頭放寬本文件的門檻。**

---

## 5. 已知會被質疑、事先說明的點

1. **世界參數仍是估計值**(`p_loiter=15%` 等)。所以本輪量到的是「相對改善量」,
   不是「絕對準確率」。改善的**方向與量級**可信,絕對值不可信。
2. **F2 是在調系統參數去貼近世界**,有「偷看答案」的嫌疑。緩解:世界的
   `tau_loiter_s=20` 固定不動,系統掃 20/45/90 —— **系統的最佳值若不是 20**
   (即不是世界的真值),才證明這不是偷看,而是「保守設定優於正確設定」的真實效果。
   這個檢查會在報告中明列。
3. **F1 幾乎必然抬高誤併率**。若最終要在碎裂與誤併之間取捨,取捨依據是
   §4 的成本比(誤併嚴重 5 倍),不是哪個數字比較好看。

---

## 6. 產出

- 程式:`src/m5_reid/evidence.py`(新增 `UnknownPathTransit` / `SameCameraTransit`)、
  `src/m5_reid/spatiotemporal.py`(`transit_llr` 支援三條新路徑)
- 設定:`configs/camera_topology.yaml` 新增 `unknown_path` / `same_camera` 區段
- 結果:`results/m5_reid/sim_after_fixes.txt`、`sim_envelope_after.json`
- 報告:併入 `docs/M5_v3_重設計與可行性驗證_20260824.md` 的續章,或另立文件

---

*本文件於實作與模擬開始前定稿。跑完後只新增「結果」與「結論」,不修改 §3 網格與 §4 判準。*
