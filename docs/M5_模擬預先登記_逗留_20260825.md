M5 逗留缺口修補 —— 模擬預先登記 第二輪(2026-08-25)

> 在跑任何模擬之前寫定並提交。網格與判準先講死,跑完只追加「結果」。
> 承接 [M5_模擬預先登記_20260825.md](M5_模擬預先登記_20260825.md)
> 的 R4/A4:F2(逗留)是三個結構缺口中唯一還沒有可行修法的。

---

## 1. 上一輪為什麼失敗

登記的旋鈕是「加大 `tau_loiter_s`」,假設「τ 越大 = 尾部越重」。**這個假設是錯的**:
指數分布是正規化的,τ 變大會把質量攤到更遠處,**在中等延遲反而密度更低**。
實測 τ=20 優於 45 與 90,與登記時的預期完全相反。

## 2. 這一輪的關鍵算術(實作前先算,免得再選錯旋鈕)

系統目前的判定式:

```
llr = log p(Δt) − log λ_bg  ≥  τ_threshold
λ_bg = 1/600 s⁻¹  →  −log λ_bg = 6.397 nats
τ_threshold = log 5 = 1.609 nats
```

所以**要通過必須** `log p(Δt) > 1.609 − 6.397 = −4.788`,即 **p(Δt) > 0.0083**。

逗留成分的**最大**密度:

| 分布 | 峰值密度 | 現值 | 夠嗎 |
|---|---|---|---|
| 指數 Exp(τ) | `p_loiter / τ` | 0.15/20 = **0.0075** | ❌ 差一點點 |
| 對數常態 LN(m, σ_L) | `p_loiter / (m·σ_L·√2π)` | 0.15/(20·1·2.507) = **0.0030** | ❌ 更差 |

**結論(登記時就先講清楚):逗留成分在 `p_loiter=0.15` 下,連自己的峰值都過不了門檻。**
換成重尾分布**不會**解決這件事 —— 重尾改善的是「很久之後」,而不是「峰值高度」。
上一輪選錯旋鈕,這一輪如果只換分布形狀,會再錯一次。

真正能動的只有兩個量:

| 旋鈕 | 物理意義 | 為什麼可以動 |
|---|---|---|
| `p_loiter`(系統端) | 系統認為「中途停下來做事」的比例 | 廚房裡停下來洗手/處理食材很常見,15% 可能低估 |
| `background_arrival_hz` | 「真正的新人」在單一鏡頭出現的速率 | **這個值是我設的,沒有量測依據**。封閉廚房固定班表下,真新人只在換班時出現,1/600s(每小時 6 位)很可能高估了一個數量級 |

⚠ `background_arrival_hz` 不是「調到過關為止」的旋鈕,而是**一個我當初隨手設定、
本來就該重新檢視的物理量**。它可被業主現場量測(數一小時有幾個不屬於任何已追蹤
廚師的人入鏡),所以這個修法是可否證的,不是擬合。

## 3. 掃描網格(事先固定)

| 參數 | 掃描值 |
|---|---|
| 逗留分布 | `exp`(現況)、`lognormal`(重尾) |
| `p_loiter`(系統端) | 0.15(現況)、0.30、0.50 |
| `background_arrival_hz` | 1/600(現況)、1/1800、1/3600 |
| `tau_loiter_s` / 對數常態中位數 | 20(固定,上一輪已證明此旋鈕無效) |

**世界參數固定不變**(與前兩輪相同,確保可比):
`n_chefs=4`、`p_loiter=0.15`、`tau_loiter_s=20`、`p_detour=0.05`、
`m4_fragment_rate=0.05`、對數常態 median 4.0 / logσ 0.35。
F3(含位置)保持開啟,F1 保持關閉。

**執行**:`--reps 12 --hours 1.5`。

## 4. 判準

沿用 [M5_模擬預先登記_20260825.md](M5_模擬預先登記_20260825.md) §4,不修改:

- 主判準:碎裂 ≤ 5% 且誤併 ≤ 1%
- 加權成本 = 碎裂 + 5 × 誤併(誤併比碎裂嚴重 5 倍)
- 次判準:正常轉場碎裂不得 > 6.0%
- 分級:A(碎裂≤5% 且誤併≤1%)/ B(≤10% 且 ≤3%)/ C(其餘)

**追加次判準(本輪特有)**:
放寬 `background_arrival_hz` 會**同時放寬所有閘門**,不只逗留。
若誤併率因此超過 **8%**(基準 4.8% 的 1.7 倍),判定該設定不可用,
即使碎裂率大幅下降。

## 5. 事先寫下的預測(可被否證)

1. **換分布形狀(exp → lognormal)單獨不會有明顯改善**,因為它不改變峰值高度。
   若實測顯示 lognormal 單獨就有效,表示我 §2 的算術有誤,要回頭檢查。
2. **`background_arrival_hz` 是主導旋鈕**,影響大於 `p_loiter`。
3. 放寬 λ_bg 會讓誤併率上升。**若碎裂率的改善換不到加權成本下降,本輪判定失敗**,
   結論將是「逗留無法在不犧牲誤併的前提下修復,需要位置或外觀以外的第三種證據」。

## 6. 已知限制

- 世界的 `p_loiter=0.15`、`tau=20` 仍是估計值。本輪量的是**相對改善**。
- 系統端 `p_loiter` 若最佳值高於世界真值(0.15),那是「保守設定優於正確設定」
  的真實效果,不是偷看答案 —— 因為世界值固定不動,系統掃描時看不到它。

---

*本文件於實作與模擬開始前定稿。*

---

# 結果(2026-08-25,跑完後追加。§3 網格與 §4 判準未修改)

執行:`python scripts/sim_m5_montecarlo.py --fixes --reps 12 --hours 1.5`
原始輸出:`results/m5_reid/sim_loiter.txt`

## R1 §2 的算術先被驗證

| λ_bg | 要通過需要 p(Δt) > | 逗留峰值(exp p=0.15) | 逗留峰值(lognormal p=0.15) |
|---|---|---|---|
| 1/600 | 0.00833 | **0.00750** ❌ | **0.00299** ❌ |
| 1/1800 | 0.00278 | 0.00750 ✅ | 0.00299 ✅ |
| 1/3600 | 0.00139 | 0.00750 ✅ | 0.00299 ✅ |

登記時算的數字與實作完全一致。**exp p=0.15 在 λ=1/600 下差 0.0008 過不了門** —— 上一輪
「碎裂 76.5%」的成因就是這 0.0008。

## R2 掃描結果(加權成本 = 碎裂 + 5×誤併)

| 設定 | 碎裂 | 誤併 | 加權成本 | vs 現況 |
|---|---|---|---|---|
| **[現況] exp p=0.15 λ=1/600** | 19.8% | 4.8% | **43.8** | — |
| exp p=0.15 λ=1/1800 | 14.2% | 6.1% | 44.7 | +0.9 |
| exp p=0.15 λ=1/3600 | 10.7% | 7.0% | 45.7 | +1.9 |
| exp p=0.30 λ=1/600 | 17.4% | 5.3% | 43.9 | +0.1 |
| exp p=0.50 λ=1/600 | 14.0% | 5.7% | 42.5 | **−1.3** |
| exp p=0.50 λ=1/3600 | 8.3% | 7.5% | 45.8 | +2.0 |
| lognormal p=0.15 λ=1/600 | 20.3% | 4.8% | 44.3 | +0.5 |
| lognormal p=0.50 λ=1/3600 | 8.1% | 7.5% | 45.6 | +1.8 |

(完整 24 列見原始輸出)

## R3 三個事先登記的預測,全部命中

| 預測 | 結果 |
|---|---|
| 1. 換分布形狀單獨不會明顯改善 | ✅ lognormal p=0.15 λ=1/600 得 **20.3%**,比現況 19.8% **還差** |
| 2. `background_arrival_hz` 是主導旋鈕 | ✅ λ 1/600→1/3600 使碎裂 19.8%→10.7%(−9.1pp);`p_loiter` 0.15→0.50 只有 19.8%→14.0%(−5.8pp) |
| 3. 若碎裂的改善換不到加權成本下降,本輪失敗 | ✅ **失敗條件成立** |

## R4 判定:本輪失敗

**沒有任何設定顯著改善加權成本。**

碎裂率可以壓到 8.1%(接近 B 級),但**每一次放寬都讓加權成本上升**:
λ 放寬三倍換到 −5.6pp 碎裂,卻換來 +1.3pp 誤併 —— 依「誤併嚴重 5 倍」的成本比,
淨值是 −5.6 + 5×1.3 = **+0.9,虧損**。

唯一為負的 `exp p=0.50 λ=1/600`(−1.3)**在雜訊內**:12 次重複下加權成本的
標準誤約 ±1.6,所以 −1.3 不顯著。

追加次判準(誤併 > 8% 判定不可用)未被觸發,最高只到 7.5% —— 但那不重要,
因為在觸發之前加權成本就已經先變差了。

**維持現況參數不變。**

## R5 這個失敗說明了什麼(比參數更重要)

逗留的本質困難是:**一個在中途停留的人,他的轉場時間與「另一個不相干的人剛好
出現」在統計上難以區分**。時間軸上這兩件事就是重疊的。

所以放寬閘門去接納逗留者,必然同時接納更多錯的人 —— 這不是參數沒調好,
是**時間這個證據軸的資訊量已經用盡**。

要突破必須引入**第三種證據軸**。已知的候選:

| 證據 | 可用性 |
|---|---|
| 外觀 | ❌ 已實證無效(DINOv2 最大 1.31 nats,同制服退化 γ=0→1 碎裂率完全不動) |
| 位置 | ⚠ 只在同鏡頭適用(跨鏡頭座標系不可比),已用在 F3 |
| **移動方向 / 出入口 zone** | ✅ **尚未實作** —— 這正是 CLM 文獻的四線索之一 |

**移動方向是唯一還沒動用的線索。** 一個從「通往 cam2 的那道門」離開、
並且從 cam2「對應那一側的入口」進場的人,不管走了多久都更可能是同一個人;
而一個不相干的人從別的方向進來,時間再吻合也可以被排除。

⚠ 諷刺的是,`camera_topology.yaml` 原本就有 `entry_zones`/`exit_zones`,
但因為**從未被任何程式讀取**,我在 2026-08-24 把它當死設定移除了
(見 M5_v3_重設計與可行性驗證_20260824.md §5)。當時的理由是「業主只有平面圖,
要求標多鏡頭出入口多邊形不合理,且沒有實測資料無從驗證有沒有幫助」。

現在有了:**模擬可以驗證它有沒有幫助**,而且如果幫助夠大,那個標註成本就值得。
這是下一輪該登記的事。
