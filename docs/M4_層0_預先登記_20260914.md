# 預先登記:層 0 追蹤碎裂 —— 偵測門檻與 stride(2026-09-14)

> **本文件在 CHIRLA 評估集上跑任何一格之前定稿並提交 git。**
> 跑完只追加 §8 之後,§1–§7 不得修改。
> 判準依 `docs/M5_主判準修正_20260912.md` 的精神:不以單一彙總數字當門檻,主判準與硬性約束分開。

---

## 1. 為什麼從層 0 開始

2026-09-13 的根因分解指出:F1/F2/F4/F5/P1/P2 六個 M5 修法**全部落在層 3/層 4**,
那兩層只佔可歸因失敗的 **11.7%**。遠端訂下的規矩第 3 條是「由下而上修」。

管線順序是 **M3 偵測 → M4 追蹤 → M5 跨鏡頭身份**。本輪處理最上游的「偵測 → 追蹤」。

依規矩第 1 條,**先說打哪一層、那一層佔多少 %** —— 見 §2。

---

## 2. 層 0 的本機拆解(2026-09-14,唯讀分析)

資料:`results/chirla_m4m5/coco_none/seq_*/track_events.csv` + `results/track_gt/`。

- 有真值的 track **2,520** 條;每位真人**中位 46 條、走過中位 7 台鏡頭**
- 「每台至少一條」之外多出 **2,200 條**(87.3%)

同一真人、同一鏡頭的相鄰兩條 track,依**後一條出生時前一條的狀態**分類:

| 類別 | 對數 | 佔比 | 間隔中位 | 性質 |
|---|---|---|---|---|
| **A** 前一條還在追,又多開一條 | 286 | 13.0% | — | 重複框或 track 漂移(分不開) |
| **B** 前一條還在 lost 緩衝內,卻另開新 ID | 682 | **31.0%** | 0.67s(p25 = 0.17s = 一個迴圈) | **M4 關聯失敗** |
| **C** 緩衝過期(>5s)後才出現 | 1,232 | 56.0% | 28s | 多半是人真的走出畫面 —— **不是 M4 的錯** |

**本輪打的層:層 0 的 B(31%)與 A(13%)。天花板約 968 條,佔 M5 被問的 3,736 次的 26%。**
C 不在本輪範圍。

---

## 3. 三個已在程式碼確認的結構問題

### 3.1 BYTE 的低分救回機制從未作用

- 網格固定 `thr=0.3`(`run_m4m5_chirla_grid.py:93`),偵測在 `model.predict(threshold=0.3)` 就被過濾
- supervision 0.28 ByteTrack 的分數帶(`core.py`):
  · 高分:`score > track_activation_threshold`(0.25)→ 第一輪關聯,IoU > 0.2
  · **低分:0.1 < score < 0.25 → 第二輪關聯,寫死 IoU > 0.5** ← 就是 BYTE
  · **開新 track:score ≥ det_thresh = 0.35**
- 進 tracker 的偵測全部 > 0.3 → **第二輪永遠是空的**

### 3.2 tracker 以 stride 5 被餵資料

`m5_track_video.py:298` 替 M5 把 fps 除以 stride,建 tracker 時沒有。
tracker 實際只看 6 fps,兩次更新之間人移動 5 倍距離。

### 3.3 removed 事件被佔位 id 灌水(已修,`36c97e1`)

7,779 次 removed 裡 4,192 次是 `track_id=-1`(從未啟動的偵測)。
**不影響本輪任何判準**(A/B/C 與 track 數都不用 removed 次數),記錄在此以免誤用舊數字。

---

## 4. 執行網格(定死)

**一次只改一個因素;緩衝固定 5 秒**(supervision 的緩衝是更新次數,換 stride 必須換算,
`eval_m4_chirla.py` 自動換算並斷言)。

| 格 | 門檻 | stride | 緩衝(次數) | 問的問題 |
|---|---|---|---|---|
| `base` | 0.30 | 5 | 30 | 回歸:必須重現 9/4 的 `coco_none`(見 V1) |
| `t025` | **0.25** | 5 | 30 | 只多收 0.25~0.30 的**高分**框,**沒有**低分帶 |
| `t010` | **0.10** | 5 | 30 | 再加上 0.10~0.25 的**低分帶** → `t010 − t025` = **BYTE 的效果** |
| `s1` | 0.30 | **1** | **150** | stride 效應 |
| `t010_s1` | 0.10 | 1 | 150 | 兩者一起 |

### ⚠ 為什麼比計畫多一格 `t025`

計畫原本只有 `low`(門檻 0.1)。但從 0.3 降到 0.1 同時改了兩件事:
多收 0.25~0.30 的高分框(進第一輪)**與**啟用 0.10~0.25 的低分帶(進第二輪)。
只跑 0.1 分不出 B 的改善來自哪一個。`t025` 把兩者拆開。

偵測器固定為 **RF-DETR nano COCO 預訓**(9/4 主基線)。微調偵測器不在本輪。

---

## 5. 判準(先寫死)

### 5.1 主判準

| # | 判準 | 方向 |
|---|---|---|
| M1 | **B 類次數** | 較 `base` 下降 |
| M2 | **每位真人每台鏡頭的 track 數**(平均) | 較 `base` 下降 |

### 5.2 硬性約束(任一不通過 → 該格不採用,不論主判準多好)

| # | 約束 |
|---|---|
| H1 | **誤偵率**較 `base` 上升不超過 **5pp** |
| H2 | **召回(全部取樣幀為分母)**不得低於 `base` |

⚠ **召回用哪個分母要講清楚**:`eval_m4m5_chirla.match_tracks` 的 `gt_total` 只累計
**有 track 的幀**,某幀有人但沒有任何 track 時那些 GT 不進分母 → 9/4 報的 88.0% 是高估的。
本輪兩種分母都報,**H2 用全部取樣幀的分母**。

### 5.3 硬性驗收(不通過就是實作或資料錯,不是效果不好)

| # | 條件 |
|---|---|
| V1 | `base` 重現 9/4 `coco_none`:track **3,736** 條、誤偵 **1,216** 條、A/B/C = **286 / 682 / 1,232**、召回(track 幀)**88.0%** |
| V2 | `cache_detections.py` 對 49 支影片 **exit 0**(每支 50 幀抽查逐位相同) |
| V3 | 每格輸出的緩衝秒數 = **5.0** |
| V4 | `src/m5_sim/world.py` 未動;既有預先登記 §1–§7 未動 |

---

## 6. 事先寫下的預測(可被否證)

| # | 預測 | 依據 |
|---|---|---|
| **1** | `t010` 的 B 較 `t025` **下降 ≥ 20%** | BYTE 低分帶正是為了接回被遮擋者;B 的 p25 間隔剛好一個迴圈 |
| **2** | `t025` 的 B 較 `base` 變動 **< 10%** | 多收 0.25~0.30 高分框不是主因 |
| **3** | `t025`、`t010` 的**誤偵 track 數**較 `base` **不增加** | 加進來的框分數都 < 0.35,**只能延長既有 track、不能開新 track** |
| **4** | `t010` 的**身份混雜 track** 較 `base` **增加** | 用低分框延長 track,容易漂到旁邊的人或物件上 |
| **5** | `s1` 的 B 較 `base` **下降 ≥ 20%** | 更新間隔縮短 5 倍,IoU 匹配更容易成功 |
| **6** | `s1` 的**誤偵 track 數**較 `base` **增加** | 未確認的 track 要連續兩次更新才啟動;stride 1 時那只要 67ms,短暫的誤偵更容易被確認 |
| **7** | **C 類次數在所有格變動 < 10%** —— **負對照** | C 是人走出畫面 >5 秒後回來,與追蹤參數無關。**大幅變動表示指標或分類有問題,先查再解讀** |

⚠ 預測 3 與 6 若同時成立,表示「降門檻」與「降 stride」對層 1 的影響方向**相反**,
兩者不能無腦疊加 —— 那正是 `t010_s1` 這一格要回答的。

### 6.1 本輪特有的失敗條件

若 **`t010` 與 `s1` 的 B 下降都 < 10%**,判定為
**「B 不是關聯參數造成的,而是偵測器在遮擋期間根本沒有輸出框」**。

處置先寫死:**不再調 tracker 參數**,改量「B 的間隔期間,該真人在那台鏡頭有 GT 框卻沒有任何偵測」
的比例(需要逐幀 GT 與快取,工具已具備),把問題移回 M3。

---

## 7. 已知限制(報告要照抄,放在結論之前)

1. **本輪只評 M4**。層 0 改善會不會傳到 M5 的層 2(其 38.6% 是「track 還在 lost 緩衝內」),
   要另跑步驟 5(完整 M5 + 四層分解)才知道。
2. **A 類分不清是重複框還是 track 漂移**。`match_tracks` 逐幀一對一再多數決。
3. **層 0 修好的天花板約 26%**。C 類 56% 不是 M4 能修的。
4. **CHIRLA 是辦公室不是廚房。**
5. 其餘 tracker 參數固定(`minimum_matching_threshold` 0.8、`track_activation_threshold` 0.25),
   本輪不掃。
6. supervision 的 `ByteTrack` **自 0.28 起已棄用、0.30 移除**;requirements 已鎖 `<0.30`。

---

## 7.1 本機已完成的工具驗證(EPFL 九台,2026-09-14)

| 項目 | 結果 |
|---|---|
| `det_cache` 事後過濾 vs torch `float32 > threshold` | 5 個門檻 × 20,003 個分數,**不一致 0 個** |
| 快取抽查(9 支 × 50 幀,門檻 0.3) | **全部逐位相同** |
| `m5_track_video.py --det-cache` vs 即時偵測基線(25 迴圈) | `chef_events.jsonl`、`tracks.csv` **逐位相同** |
| `eval_m4_chirla.py` 從快取驅動 tracker vs 同一基線 | track **251/251**、事件 **12/12** 逐列相同,**1 秒** |

⚠ 本機沒有 CHIRLA,V1 必須在遠端確認。

---

## 7.2 遠端指令

```bash
set PYTHONIOENCODING=utf-8        # 遠端 stdout 是 cp950,不設會在 ≥ 等字元掛掉

# 1) 建快取(上界約 4.4 小時,可續跑;每支影片自動抽查)
python scripts/cache_detections.py --chirla-root "D:/新增資料夾/CHIRLA/CHIRLA_data/CHIRLA" ^
    --out-dir results/det_cache/coco_nano

# 2) 網格(每格只有追蹤)
set R=D:/新增資料夾/CHIRLA/CHIRLA_data/CHIRLA
set S=seq_004 seq_006 seq_007 seq_020 seq_024 seq_025 seq_026
python scripts/eval_m4_chirla.py --cache-dir results/det_cache/coco_nano --root %R% --seqs %S% --thr 0.30 --stride 5 --lost-buffer-seconds 5 --label base
python scripts/eval_m4_chirla.py --cache-dir results/det_cache/coco_nano --root %R% --seqs %S% --thr 0.25 --stride 5 --lost-buffer-seconds 5 --label t025
python scripts/eval_m4_chirla.py --cache-dir results/det_cache/coco_nano --root %R% --seqs %S% --thr 0.10 --stride 5 --lost-buffer-seconds 5 --label t010
python scripts/eval_m4_chirla.py --cache-dir results/det_cache/coco_nano --root %R% --seqs %S% --thr 0.30 --stride 1 --lost-buffer-seconds 5 --label s1
python scripts/eval_m4_chirla.py --cache-dir results/det_cache/coco_nano --root %R% --seqs %S% --thr 0.10 --stride 1 --lost-buffer-seconds 5 --label t010_s1
```

**先看 `base` 是否通過 V1,再看其他格。** 不通過就停下來查,不要解讀其他格。

---

*本文件於在 CHIRLA 評估集上跑任何一格之前定稿。跑完只追加 §8「結果」與其後章節。*

---

# 8. 結果(2026-09-14,遠端 Venus / RTX A6000)

> 本節之後為跑完才追加的內容。§1–§7 未修改(提交前以 `git diff --numstat` 確認 0 刪除)。

## 8.1 一句話結論

**調追蹤參數救不了 B 類;但原因不是偵測器沒看到人,而是 ByteTrack 只靠框的重疊度配對,接不回小而移動中的人。**
§6.1 的失敗條件已觸發,而它寫死的後續量測**否定了「問題在 M3」的假說** —— 問題留在 M4 的關聯方法。

## 8.2 執行與硬性驗收

| # | 條件 | 實測 | 判定 |
|---|---|---|---|
| V2 | 快取 49 支 exit 0、每支 50 幀逐位相同 | 254.0 分鐘,49 支全部「不同 0 幀」 | **通過** |
| V3 | 每格緩衝 5.0 秒 | 五格皆 5.0(stride 1 為 150 次更新) | **通過** |
| V4 | `world.py` 與既有預先登記 §1–§7 未動 | 是 | **通過** |
| V1 | 見 8.3 | 字面未完全吻合 | **經使用者裁定為實質通過** |

⚠ 快取建置期間 ffmpeg 輸出約 26.9 萬行 `marker does not match f_code` 與 2 行 `Error at MB`(CHIRLA `.avi` 部分影格資料有瑕疵)。
這與 9/4 起所有網格走**同一條解碼路徑**(`iter_frames` → `cv2.VideoCapture`),當時被 `run_m4m5_chirla_grid.py` 的 `capture_output=True` 吞掉而沒進日誌。V1 的逐筆比對證明解碼行為與 9/4 相同。

## 8.3 V1:字面數字差 4 對與 1 個框,追蹤本身逐筆相同

| 項目 | V1 要求 | 實測 |
|---|---|---|
| track 總數 | 3,736 | 3,736 |
| 誤偵 track | 1,216 | 1,216 |
| A / B / C | 286 / 682 / 1,232 | **287 / 683 / 1,230** |
| 召回(track 幀) | 88.0% | **87.9%** |

查證:

1. **追蹤事件與每幀每條 track 的框,與 9/4 `results/chirla_m4m5/coco_none` 原檔逐筆相同**
   (原檔扣掉已修掉的 `track_id=-1` removed 事件 4,192 列;各序列 564 / 179 / 144 / 1,237 / 860 / 640 / 568)。
2. **A/B/C**:用同一個 `eval_m4_chirla.classify_pairs` 算 9/4 原檔,也是 **287 / 683 / 1,230**。
   → §2 寫的 286 / 682 / 1,232 **用 repo 內的資料與函式重現不出來**,是登記目標值的問題,不是工具。
3. **召回**:156,873 個真值框中配到 137,969(9/4 為 137,970),差 1 個,恰在 87.95% 的四捨五入邊界。
   9/4 以 CSV 小數 1 位的框算 IoU,本工具在記憶體以未四捨五入的框算;把本次 track 匯出成 1 位小數再算即為 137,970。

**使用者裁定:視為實質通過。** 以下所有比較一律以**本次重播的 `base`** 為基準。

## 8.4 五格總表

| 格 | track | 誤偵 track | 誤偵率 | 召回(全部幀) | 召回(track 幀) | A | B | C | 混雜 track | 每人每台 track |
|---|---|---|---|---|---|---|---|---|---|---|
| `base` | 3,736 | 1,216 | 32.55% | 87.03% | 87.9% | 287 | 683 | 1,230 | 411 | 7.875 |
| `t025` | 3,695 | 1,205 | 32.61% | 87.71% | 88.6% | 276 | 665 | 1,230 | 411 | 7.806 |
| `t010` | 3,868 | 1,374 | 35.52% | 88.91% | 89.6% | 307 | 649 | 1,218 | 403 | 7.794 |
| `s1` | 7,574 | 3,718 | 49.09% | 91.18% | 91.7% | 902 | 1,141 | 1,490 | 384 | 11.938 |
| `t010_s1` | 8,025 | 4,263 | 53.12% | 93.42% | 93.7% | 929 | 1,038 | 1,472 | 357 | 11.647 |

## 8.5 判準

| 格 | M1 B | M2 每人每台 | H1 誤偵率(≤ +5pp) | H2 召回全幀(≥ base) | 判定 |
|---|---|---|---|---|---|
| `t025` | −2.6% 是 | 7.806 是 | +0.06pp 過 | +0.68pp 過 | 形式上可採用,改善微小 |
| `t010` | −5.0% 是 | 7.794 是 | +2.97pp 過 | +1.88pp 過 | 形式上可採用,改善微小 |
| `s1` | +67.1% 否 | 11.938 否 | **+16.54pp 不過** | +4.15pp 過 | **不採用** |
| `t010_s1` | +52.0% 否 | 11.647 否 | **+20.57pp 不過** | +6.39pp 過 | **不採用** |

## 8.6 負對照(預測 7)失效的調查

C 類:`t025` +0.0%、`t010` −1.0%(可解讀);**`s1` +21.1%、`t010_s1` +19.7%(失效)**。依規定先查分類再解讀:

- **分類不變量成立**:兩格的「同一真人同一鏡頭相鄰配對總數」= A+B+C,未分類 0(`base` 2,200、`s1` 3,530)
- **多出的 C 來自短碎片**:C 類配對的「前一條 track 存活長度」,`s1` 相對 `base`:≤0.5 秒 +143、0.5~2 秒 +79、2~10 秒 +100、>10 秒 −62
- 機制:stride 1 時未確認的候選只要連續兩次更新(67 毫秒)就被確認成 track,短命碎片變多,人 5 秒後再被偵測到就多一個 C

→ **不是分類錯誤,而是 §6 預測 7 的前提「C 與追蹤參數無關」在碎片數量大幅變動時不成立。**
`s1` / `t010_s1` 不論如何都因 H1 不採用,這個判定用真值配對,與 A/B/C 分類無關。
(附帶:`s1` 逐條匯出重算的 A/B 為 900 / 1,140,與 json 的 902 / 1,141 差 3,C 相同,屬 8.3 的同類精度差。)

## 8.7 預測逐條

| # | 預測 | 實測 | 判定 |
|---|---|---|---|
| 1 | `t010` 的 B 較 `t025` 下降 ≥ 20% | −2.4% | **不成立** |
| 2 | `t025` 的 B 較 `base` 變動 < 10% | −2.6% | 成立 |
| 3 | `t025`、`t010` 誤偵 track 不增加 | `t025` −11;**`t010` +158** | **不成立**(`t010` 增加) |
| 4 | `t010` 混雜 track 增加 | −8 | **不成立** |
| 5 | `s1` 的 B 較 `base` 下降 ≥ 20% | **+67.1%** | **不成立**(方向相反) |
| 6 | `s1` 誤偵 track 增加 | +2,502 | 成立 |
| 7 | C 類所有格變動 < 10% | `s1` +21.1%、`t010_s1` +19.7% | **不成立**(見 8.6) |

⚠ §6 的附註「預測 3 與 6 同時成立 → 兩者方向相反」不適用:預測 3 在 `t010` 不成立,降門檻與每幀更新**都**增加誤偵 track,只是後者多得多。

## 8.8 §6.1 失敗條件:**觸發**

`t010` 的 B 下降 5.0%、`s1` 的 B **增加** 67.1%,兩者都未達「下降 ≥ 10%」。

### 8.8.1 寫死的後續量測:B 類間隔期間,偵測器有沒有給框

對 `base` 的 683 個 B 類斷點,在「前一條 track 跟丟」到「新 track 出生」之間,
凡真值標註說該真人在那台鏡頭畫面裡的幀,從快取找與真值框 IoU ≥ 0.5(全專案評估標準)的偵測框,記最高分。

**全部幀**(快取逐幀):

| 最佳框分數 | 幀數 | 佔比 |
|---|---|---|
| 無框 | 833 | 6.0% |
| 0.05~0.10 | 299 | 2.2% |
| 0.10~0.25 | 1,635 | 11.8% |
| 0.25~0.30 | 745 | 5.4% |
| **≥ 0.30** | **10,313** | **74.6%** |

以斷點計:≥ 0.30 有 585(85.7%)、整段無框 **0**、間隔內真值都說人不在 87(12.7%)。

**只算 `base` 追蹤器實際看到的幀(stride 5)**:

| 最佳框分數 | 幀數 | 佔比 |
|---|---|---|
| 無框 | 159 | 6.8% |
| 0.05~0.10 | 53 | 2.3% |
| 0.10~0.25 | 298 | 12.7% |
| 0.25~0.30 | 138 | 5.9% |
| **≥ 0.30** | **1,703** | **72.4%** |

以斷點計:≥ 0.30 有 408(59.7%)、整段無框 **2(0.3%)**、間隔內沒有追蹤器看得到且人在畫面的幀 256(37.5%,多為新 track 在下一次更新就出生)。

→ **§6.1 的假說「偵測器在遮擋期間根本沒有輸出框」被否定。** 偵測器多數時候看得到人且分數夠高;
**問題不移回 M3,留在 M4 的關聯。**

### 8.8.2 補充診斷(非預先登記):為什麼沒接上

比較「舊 track 跟丟前最後一個**真實配對**框」與「新 track 出生時第一個框」的 IoU(683 個):

| IoU | 斷點數 | 佔比 |
|---|---|---|
| < 0.2 | 344 | 50.4% |
| 0.2~0.5 | 224 | 32.8% |
| 0.5~0.8 | 94 | 13.8% |
| ≥ 0.8 | 21 | 3.1% |

IoU 中位 **0.199**;間隔中位 25 幀(約 0.83 秒);新框分數中位 0.709(p10 0.394);舊框寬中位 **65 px**。

讀法:ByteTrack 第一輪要求 IoU × 分數 ≥ 0.2,分數 0.709 時需 IoU ≥ 0.28。**一半的斷點連與舊框的 IoU 都不到 0.2** ——
CHIRLA 的人框小,隔不到一秒人一移動就幾乎不重疊,純 IoU 的關聯接不回來。

⚠ 這是**代理量**:ByteTrack 實際比的是卡爾曼**預測**框,不是最後真實框。
另有 16.9% 的斷點 IoU ≥ 0.5 仍沒接上,重疊度解釋不了;可能是跟丟期間卡爾曼沿舊速度外推而漂移,**未驗證**。

## 8.9 這一輪不能回答什麼(§7 照抄並補充)

1. **本輪只評 M4。** 層 0 改善會不會傳到 M5 的層 2,要另跑完整 M5 + 四層分解
2. **A 類分不清是重複框還是 track 漂移**
3. **層 0 修好的天花板約 26%**,C 類 56% 不是 M4 能修的
4. **CHIRLA 是辦公室不是廚房**
5. 其餘 tracker 參數固定,本輪不掃
6. supervision 的 `ByteTrack` 自 0.28 棄用、0.30 移除;requirements 已鎖 `<0.30`
7. **(補)V1 字面未完全吻合**,經使用者裁定為實質通過(8.3)
8. **(補)預測 7 的前提在 stride 變動時不成立**,C 類不是跨 stride 的有效負對照(8.6)
9. **(補)8.8.2 的機制分析是代理量且非預先登記**;「有 ≥ 0.30 的框」不等於「追蹤器配得上」

## 8.10 下一步(不在本文件判定範圍,只記錄)

1. **不再調 ByteTrack 的門檻與 stride。** 最好的 `t010` 只讓 B 降 5.0%,且誤偵 track 多 158 條
2. **層 0 的修法對象是跟丟期間的關聯方法**,不是偵測器。候選(需另寫預先登記):
   - **OC-SORT**:以觀測修正跟丟期間的卡爾曼漂移,純運動、無外觀,**授權乾淨**;直接對應 8.8.2 那 16.9%
   - 放寬跟丟 track 的配對範圍(以中心距離或擴張框取代純 IoU):直接對應 IoU < 0.2 的 50.4%
   - 帶外觀的追蹤器(BoT-SORT、Deep OC-SORT)⚠ 會碰到與 M5 相同的外觀模型授權問題(DINOv2 僅 0.062 nats)
3. 追蹤器本來就必須換:supervision 0.30 將移除 `ByteTrack`
4. `t010` 是否作為下一輪基線,在下一份預先登記決定(形式上通過全部判準,但效果微小)

## 8.11 本機補充(2026-09-15,非預先登記;只追加)

### 8.11.1 §2 的 286 / 682 / 1,232 從哪來(回覆遠端的待確認)

是**我 9/14 臨時分析程式的排序方式**,不是資料或工具的問題:
它把**同一時間出生**的 track 以 **track_id 字串**排序(`"10" < "9"`),而 `classify_pairs` 保留 tracker 發事件的順序。
七個序列裡同一真人、同一鏡頭、同時出生的有 14 組,其中幾組的前後順序因此對調,淨效果是 A −1、B −1、C +2(沒有逐對追是哪幾對換了類別)。
本機兩種排序都跑過:字串排序得 286 / 682 / 1,232,事件順序得 **287 / 683 / 1,230**(與遠端一致)。
→ V1 的「字面差 4 對」在登記目標值那一側;**後續一律以 287 / 683 / 1,230 為準**。

### 8.11.2 B 佔相鄰配對的比例五格幾乎不動

§8.4 的 B 絕對數會被「配對總數」帶著走 —— stride 1 碎片變多,配對總數跟著暴增。換成佔比:

| 格 | A+B+C | B | **B 佔比** |
|---|---|---|---|
| `base` | 2,200 | 683 | **31.0%** |
| `t025` | 2,171 | 665 | **30.6%** |
| `t010` | 2,174 | 649 | **29.9%** |
| `s1` | 3,533 | 1,141 | **32.3%** |
| `t010_s1` | 3,439 | 1,038 | **30.2%** |

`s1` 的 B「+67%」幾乎全是配對總數 +61% 帶來的。**調門檻與 stride 沒有改變「斷掉時是 B 類」的傾向**,
與 §8.8.2 的機制(純 IoU 接不回)一致 —— 下一輪要換的是配對方法本身。
下一輪起 `eval_m4_chirla.py` 直接輸出 `b_share`,跨格比較以佔比為主。

### 8.11.3 §3.1 的第一輪門檻寫錯

§3.1 寫「高分 → 第一輪關聯,IoU > 0.2」。supervision 0.28 實際是 `matching.fuse_score` 之後比:
**IoU × 偵測分數 > 0.2**(`minimum_matching_threshold` 0.8 → 成本 < 0.8)。§8.8.2 的「分數 0.709 時需 IoU ≥ 0.28」是對的。
本輪網格只改門檻與 stride,不受這個描述錯誤影響;但它**讓純 IoU 對小框更不利**(低分框要更高的 IoU 才配得上)。

### 8.11.4 §8.8.1 / §8.8.2 的分析補成腳本

9/14 的兩個關鍵量測是唯讀臨時分析、沒有提交腳本。已補 `scripts/diag_m4_breaks.py`(旗標與 `eval_m4_chirla.py` 相同),
**遠端先以 `base` 重現 72.4% / 0.199 / 50.4% / 16.9% 再用於下一輪**。
