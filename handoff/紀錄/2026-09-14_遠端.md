# 2026-09-14 ・ 遠端 ・ 宜蓁(Claude)

## 這次做了什麼

**跑完層 0(M4 追蹤碎裂)五格網格。結論:調追蹤參數救不了 B 類,但原因不是偵測器,是 ByteTrack 只靠框的重疊度接不回小而移動中的人。**

完整結果追加在 `docs/M4_層0_預先登記_20260914.md` §8(§1–§7 未修改)。

1. 建偵測快取(49 支、254 分鐘,每支抽 50 幀逐位比對全部通過)
2. 跑 `base` 驗 V1 → 字面差 4 對與 1 個框,**查證後由使用者裁定為實質通過**
3. 跑 `t025` / `t010` / `s1` / `t010_s1`(每格 23~124 秒)
4. 負對照在 stride 1 兩格失效 → 查證不是分類錯誤
5. §6.1 失敗條件觸發 → 跑寫死的後續量測,**否定了「問題在 M3」**
6. 補充診斷沒接上的原因
7. 另外把 9/13 根因分解被推翻的兩個數字更正進文件(`0ccb223`)

| 格 | B | 誤偵率 | 召回(全部幀) | 每人每台 track | 判定 |
|---|---|---|---|---|---|
| `base` | 683 | 32.55% | 87.03% | 7.875 | 基準 |
| `t025` | 665(−2.6%) | 32.61% | 87.71% | 7.806 | 形式上通過,改善微小 |
| `t010` | 649(−5.0%) | 35.52% | 88.91% | 7.794 | 形式上通過,改善微小 |
| `s1` | 1,141(+67.1%) | **49.09%** | 91.18% | 11.938 | 不採用(誤偵率超限) |
| `t010_s1` | 1,038(+52.0%) | **53.12%** | 93.42% | 11.647 | 不採用(誤偵率超限) |

**B 類斷點期間偵測器有沒有給框**(`base` 的 683 個斷點,只算追蹤器實際看到的幀):
真人在畫面裡時 **72.4% 的幀有 ≥ 0.30 分、與真值 IoU ≥ 0.5 的框**;整段無框只有 **2 個(0.3%)**。

**為什麼沒接上**:舊 track 最後真實框 vs 新 track 第一個框的 IoU **中位 0.199**,一半 < 0.2;人框寬中位 65 px,間隔中位 0.83 秒。

## 實際跑的指令與結果

```bash
export PYTHONIOENCODING=utf-8
R="D:/新增資料夾/CHIRLA/CHIRLA_data/CHIRLA"
S="seq_004 seq_006 seq_007 seq_020 seq_024 seq_025 seq_026"

python scripts/cache_detections.py --chirla-root "$R" --out-dir results/det_cache/coco_nano

python scripts/eval_m4_chirla.py --cache-dir results/det_cache/coco_nano --root "$R" --seqs $S \
    --thr 0.30 --stride 5 --lost-buffer-seconds 5 --label base
# 同上,依序換成:--thr 0.25 --stride 5 --label t025
#                 --thr 0.10 --stride 5 --label t010
#                 --thr 0.30 --stride 1 --label s1
#                 --thr 0.10 --stride 1 --label t010_s1

# V1 查證與 §6.1 後續量測用 --dump-dir 匯出 base / s1 的逐條 track,另以唯讀 python 分析
```

```
快取:完成,254.0 分鐘。全部逐位比對通過(49 支 ✅,不同 0 幀)
base:3736 tracks  誤偵 32.5%  召回(track幀)87.9%  (全部幀)87.0%  A 287 B 683 C 1230
V1 追蹤事件逐筆比對:7 序列全部相同(9/4 原檔扣 track_id=-1 共 4,192 列後)
classify_pairs 對 9/4 原檔:A 287 B 683 C 1230(與預先登記 286/682/1,232 不同)
```

## 遇到的問題與怎麼解的

| 症狀 | 原因 | 解法 |
|---|---|---|
| 快取日誌 26.9 萬行 `marker does not match f_code` | CHIRLA `.avi` 部分影格資料有瑕疵;與 9/4 起同一解碼路徑,當時被網格腳本 `capture_output=True` 吞掉 | 不處理;V1 逐筆比對證明解碼行為與 9/4 相同 |
| 快取日誌看不到每支影片的抽查結果 | Python 輸出導向檔案時整塊緩衝;抽查失敗**不會立刻中止**,跑完才總結 | 等最後一行「全部逐位比對通過」 |
| V1 的 A/B/C 差 4 對 | 預先登記 §2 的數字用 repo 內資料與函式重現不出來 | 追蹤逐筆相同,使用者裁定實質通過,基準改用本次重播的 `base` |
| V1 召回 87.9% ≠ 88.0% | 差 1 個真值框;未四捨五入 vs 小數 1 位的框算 IoU | 同上 |
| stride 1 兩格 C 類 +20% | 短碎片 track 變多(≤2 秒的前一條 track +222 對) | 分類不變量成立,不是錯誤;兩格反正因誤偵率不採用 |
| 推送失敗(403) | 認證管理員存的是 `yee645`;PAT 原約定單次使用 | 本輪未推送,等使用者決定 |

## 環境資訊

| 項目 | 值 |
|---|---|
| 機器 | Venus |
| Python | 3.11.9(`.venv`) |
| torch / CUDA | driver 12.8 → cu128 |
| GPU 型號 / VRAM | NVIDIA RTX A6000 / 49,140 MiB |
| 磁碟餘裕 | D: 約 1.1 TB |
| CHIRLA 路徑 | `D:/新增資料夾/CHIRLA/CHIRLA_data/CHIRLA` |
| supervision | 0.29.1 |

## 待確認 / 卡住的地方

- **推送**:本地有未推送的 commit,等使用者決定推送方式(PAT 原約定單次使用,已請撤銷)
- 預先登記 §2 的 A/B/C = 286 / 682 / 1,232 是怎麼算出來的,**請本機確認**
- 8.8.2 那 16.9%「IoU ≥ 0.5 仍沒接上」可能是卡爾曼漂移,**未驗證**

## 下一步

1. **不再調 ByteTrack 門檻與 stride**
2. 層 0 的修法對象是**跟丟期間的關聯方法**,要另寫預先登記。候選:OC-SORT(純運動、授權乾淨)、放寬跟丟 track 的配對範圍;帶外觀的追蹤器會碰到外觀模型授權問題
3. 追蹤器本來就必須換(supervision 0.30 移除 `ByteTrack`)
