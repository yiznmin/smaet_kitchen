# 2026-09-15 ・ 遠端 ・ 宜蓁(Claude)

## 這次做了什麼

**跑完層 0 第二輪(換掉跟丟期間的配對方法)。結論:`cbiou` 勝出,B 佔比 29.85% → 20.14%;但約四成改善來自換套件本身。**

完整結果追加在 `docs/M4_層0_關聯方法_預先登記_20260915.md` §8(§1–§7 未修改)。

1. `trackers==2.6.0` 安裝前 dry-run → 只新增這一個套件,numpy 2.4.6 不需另建 venv
2. `verify_m4.py` 四個 backend + S6 **ALL PASS**(V4)
3. 回歸與工具驗收:`base`、`t010` 與 9/14 提交版 json **共同 83 欄位 0 差異**(V1、V2);`diag_m4_breaks.py` 重現 72.4% / 0.199 / 50.4% / 16.9%(V3)
4. 網格 `rf_botsort` / `cbiou` / `ocsort` / `cbiou_rerun` + 三格斷點診斷
5. V5(以建構子預設值補查 `ocsort` 參數)、V6(兩次 `cbiou` 逐位相同)、V7 全部通過

| 格 | B 佔比 | 每人每台 track | 誤偵率 | 召回(全部幀) | 混雜 track | 判定 |
|---|---|---|---|---|---|---|
| `t010`(比較對象) | 29.85% | 7.794 | 35.52% | 88.91% | 403 | — |
| `rf_botsort`(橋樑格) | 25.69% | 7.594 | 33.37% | 91.50% | 335 | — |
| **`cbiou`** | **20.14%** | **6.792** | 34.01% | **92.35%** | 389 | **勝出** |
| `ocsort` | 35.81% | 8.368 | 35.31% | 89.12% | 480 | 不採用(M1、M2、H3 不過) |

三件要記住的事:

1. **改善的來源要拆開看**:−9.71pp = 換套件 −4.17pp + 框放大 −5.55pp。沒有橋樑格就會全部算給 C-BIoU
2. **預測 4 字面成立,機制讀法未獲支持**:框放大後各重疊段的斷點**等比例**減少(< 0.2 段佔比 49.6% → 49.9% 沒變),不是專救低重疊斷點
3. **`ocsort` 在框仍高度重疊時就換 id**(剩下的 B 裡 IoU ≥ 0.5 佔 28.2%、間隔中位 10 幀),原因未驗證

相對目前預設 `base`:track 3,736 → **3,273(−12.4%)**,M5 要回答「這是誰」的次數跟著減少。

## 實際跑的指令與結果

```bash
export PYTHONIOENCODING=utf-8
python -m pip install trackers==2.6.0 --dry-run     # Would install trackers-2.6.0(其餘皆已滿足)
python -m pip install trackers==2.6.0
python scripts/verify_m4.py                           # [ALL PASS]

R="D:/新增資料夾/CHIRLA/CHIRLA_data/CHIRLA"
S="seq_004 seq_006 seq_007 seq_020 seq_024 seq_025 seq_026"
C="--cache-dir results/det_cache/coco_nano --root $R --seqs $S --stride 5 --lost-buffer-seconds 5"

python scripts/eval_m4_chirla.py $C --thr 0.30 --label base
python scripts/eval_m4_chirla.py $C --thr 0.10 --label t010
python scripts/diag_m4_breaks.py $C --thr 0.30 --label base
python scripts/eval_m4_chirla.py $C --thr 0.10 --backend rf_botsort --label rf_botsort
python scripts/eval_m4_chirla.py $C --thr 0.10 --backend rf_cbiou   --label cbiou
python scripts/eval_m4_chirla.py $C --thr 0.10 --backend rf_ocsort  --label ocsort
python scripts/eval_m4_chirla.py $C --thr 0.10 --backend rf_cbiou   --label cbiou_rerun
python scripts/diag_m4_breaks.py $C --thr 0.10 --backend rf_botsort --label rf_botsort
python scripts/diag_m4_breaks.py $C --thr 0.10 --backend rf_cbiou   --label cbiou
python scripts/diag_m4_breaks.py $C --thr 0.10 --backend rf_ocsort  --label ocsort
```

```
base        track 3,736  B 佔比 31.05%  召回全幀 87.03%   與 9/14 共同 83 欄位 0 差異
t010        track 3,868  B 佔比 29.85%  召回全幀 88.91%   與 9/14 共同 83 欄位 0 差異
diag_base   IoU <0.2 344 / 0.2~0.5 224 / 0.5~0.8 94 / ≥0.8 21;中位 0.199
rf_botsort  track 3,647  B 佔比 25.69%  (18.5 秒)
cbiou       track 3,273  B 佔比 20.14%  (19.5 秒)
ocsort      track 4,152  B 佔比 35.81%  (21.2 秒)
cbiou_rerun 去掉 seconds/label 後與 cbiou 逐位相同
```

## 遇到的問題與怎麼解的

| 症狀 | 原因 | 解法 |
|---|---|---|
| `ocsort` 的 json `tracker_kwargs` 只有 3 個鍵,V5 無法直接核對 | json 只記明確傳入的參數,套件預設不寫入 | 以 `inspect.signature(OCSORTTracker.__init__)` 查證預設值與 §3.1 一致;建議日後寫出完整生效參數 |
| 診斷腳本的間隔中位 20 幀 ≠ 9/14 手算 25 幀 | 起點定義不同(跟丟事件那一幀 vs 最後真實配對那一幀),恰差一個 stride | 不在 V3 判準內,§8.3 照實記錄 |
| `base.json` / `t010.json` 被覆寫成「modified」 | 本機這輪新增 8 個欄位(`b_share`、`backend`、`tracker_kwargs` 等) | 共同欄位逐一比對 0 差異,連同新欄位一起提交 |

## 環境資訊

| 項目 | 值 |
|---|---|
| 機器 | Venus |
| Python | 3.11.9(`.venv`) |
| numpy / supervision / trackers | 2.4.6 / 0.29.1 / **2.6.0(本日新裝)** |
| pyDeprecate | 0.9.0 |
| GPU 型號 / VRAM | NVIDIA RTX A6000 / 49,140 MiB(本輪只跑追蹤,未用 GPU) |
| CHIRLA 路徑 | `D:/新增資料夾/CHIRLA/CHIRLA_data/CHIRLA` |

## 待確認 / 卡住的地方

- 步驟 3 前要確認:`m5_track_video.py` 能否以 `--det-cache` 搭配 `rf_cbiou` backend 跑;`--track-gt` 的真值表要用 `cbiou` 的 track 重算(track id 與 `base` 不同)
- `ocsort` 更碎的原因未驗證

## 下一步

1. **依 §5.2,`cbiou` 進步驟 3**:完整 M5 + 四層分解(要先寫預先登記),看 9/13 層 2「track 還在緩衝內」的 38.6% 有沒有縮小
2. `cbiou` 仍剩 A + B 612 對;若層 2 沒縮小,改由 M5 側的候選路徑 (c) 處理

---

# 續:推導集保留集確認 —— `cbiou` 確認成立(但主要由 seq_000 撐起)

完整結果在 `docs/M4_層0_保留集確認_預先登記_20260915.md` §8(預先登記 `7c48853` 在跑任何評估前已提交並推送)。

**為什麼做**:`cbiou` 是在評估集 7 個序列上從兩個候選中挑出的最好者,同一批資料上挑選會偏樂觀。推導集 seq_000/001/002 從未參與任何 M4 決策。

| 格(推導集 3 序列) | B 佔比 | 每人每台 track | 誤偵率 | 召回(全部幀) | 混雜 |
|---|---|---|---|---|---|
| `d_t010` | 29.74% | 4.494 | 33.84% | 92.99% | 38 |
| `d_rf_botsort` | 25.98% | 4.256 | 33.47% | 94.05% | 34 |
| **`d_cbiou`** | **18.64%** | **3.857** | 35.15% | **94.54%** | 36 |

- **M1、M2、H1、H2、H3 全部通過 → 確認成立**;V1~V5 通過(推導集快取 61.3 分鐘、21 支抽查 0 幀不同;參數與評估集逐項相同)
- 預測 1~3 成立;**預測 4(贏家詛咒)不成立**:降幅 11.10pp > 評估集 9.71pp
- 改善拆解與評估集一致:換套件 −3.76pp、框放大 −7.35pp(評估集 −4.17 / −5.55)
- ⚠ **推導集配對 81% 來自 seq_000**;seq_001 只有 7~11 對,且該序列上 `cbiou` 的 B 佔比**高於**橋樑格(3 對 vs 2 對)

## 實際跑的指令

```bash
export PYTHONIOENCODING=utf-8
R="D:/新增資料夾/CHIRLA/CHIRLA_data/CHIRLA"
python scripts/cache_detections.py --chirla-root "$R" --seqs seq_000 seq_001 seq_002 --out-dir results/det_cache/coco_nano
C="--cache-dir results/det_cache/coco_nano --root $R --seqs seq_000 seq_001 seq_002 --stride 5 --lost-buffer-seconds 5"
python scripts/eval_m4_chirla.py $C --thr 0.30                      --label d_base
python scripts/eval_m4_chirla.py $C --thr 0.10                      --label d_t010
python scripts/eval_m4_chirla.py $C --thr 0.10 --backend rf_botsort --label d_rf_botsort
python scripts/eval_m4_chirla.py $C --thr 0.10 --backend rf_cbiou   --label d_cbiou
# V4 逐序列:同上三格各以 --seqs <單一序列> 輸出到暫存目錄
```

## 步驟 3 的準備檢查(唯讀)

`eval_m4_chirla.py` 與 `m5_track_video.py` 都經由 `KitchenTracker.from_config()` → `rf_kwargs()` 建追蹤器,
M5 runner 用一份 `backend: rf_cbiou` 的 tracker YAML 即可得到與 `cbiou` 格相同的參數,**不需改程式**。
要注意:真值對照表要用 `cbiou` 的 track 重算;runner 的 `run_meta.json` 是否記下實際後端需確認。

## 下一步

**步驟 3:`cbiou` 接完整 M5 + 四層分解**(要先寫預先登記),看層 2「track 還在緩衝內」的 38.6% 有沒有縮小。

---

# 續二:步驟 3 —— 層 0 的改善傳到了 M5(但正確率沒升)

完整結果在 `docs/M5_步驟3_追蹤器傳導_預先登記_20260915.md` §8(預先登記與工具 `65c704a` 在跑之前已提交)。

**問的問題**:M4 少斷了,M5 是不是真的少錯?

| 格 | 決策 | L2 不在任何畫面 | L2 在不重疊的別台 | IDF1 | 正確率 | 誤併率 | 誤偵綁定率 |
|---|---|---|---|---|---|---|---|
| `m5_base` | 3,736 | 391 | 550 | 0.1512 | 10.44% | 58.23% | 32.55% |
| `m5_t010` | 3,868 | 376 | 550 | 0.1475 | 10.14% | 58.54% | 35.50% |
| `m5_rf_botsort` | 3,647 | 332 | 569 | 0.1510 | 10.41% | 58.60% | 33.34% |
| **`m5_cbiou`** | **3,273** | **248** | 525 | **0.1574** | 10.00% | 59.54% | 34.01% |

- **M1、M2、H1、H2 全部通過** → 決策表第一列「層 0 改善傳到 M5」;V1~V5 全部通過
- 負對照成立:拓撲漏洞那一類 550 → 525(−4.5%)
- 「不在任何畫面」−143 的拆解:降門檻 −15、換套件 −44、框放大 −84
- 預測 1、2、4、5、6 成立;預測 3(「就在本鏡頭」下降)不成立,71 → 76
- ⚠ **正確率 10.44% → 10.00%**(判準沒涵蓋):IDF1 升而正確率降,分母不同(可歸因 2,461 → 2,101)。不能說「綁對得更多」
- ⚠ IDF1 只 +0.0062;誤併率 59.54%,離 ≤ 1% 非常遠
- ⚠ 只降門檻在 M5 上變差(IDF1 0.1475)

## 新增 / 修改的工具(已在 9/13 資料上驗證後才用)

| 檔案 | 用途 | 驗證 |
|---|---|---|
| `scripts/diag_m5_layers.py`(新) | 四層分解、交叉表、L2 成因的正式工具 | 對 9/13 `chirla_diag/base` 逐項重現 |
| `scripts/make_track_gt.py` | `--tracks-dir`:從 M4 匯出算真值表 | 內容與 `results/track_gt` 完全相同 |
| `scripts/m5_track_video.py` | `run_meta` 記 `tracker_path` / `tracker_cfg` | V3 |
| `configs/tracker_rf_botsort.yaml`、`tracker_rf_cbiou.yaml` | 衍生設定檔 | 解析後只差 `backend` |

## 遇到的問題與怎麼解的

| 症狀 | 原因 | 解法 |
|---|---|---|
| `diag_m5_layers.py` 第一版對帳判 FAIL | `summarize` 的比率已四捨五入到 4 位,我用 1e-9 容許誤差比 | 改成同樣四捨五入到 4 位比對 |
| 新模式真值表 md5 與 9/13 不同 | M4 評估逐台鏡頭輸出 track,JSON 鍵順序不同 | 解析後比對內容:七序列完全相同 |
| V1 時 `track_events.csv` 七序列都不同 | 9/13 檔含 4,192 列 `-1`;扣掉後 seq_004/024 有同迴圈內 `removed` 換序(集合走訪順序) | 不在判準內;`chef_events` 逐位相同證明不影響決策 |
| 設定檔換行字元 LF(原檔 CRLF) | `sed` 輸出 | 解析後內容相同,不影響 |
| 預估每格 1 小時 | 讀快取後解碼很快 | 實測約 10 分鐘 |

## 中途回答使用者:`claude rc`

`claude rc` = `claude remote-control`,可從 claude.ai/code 或 Claude 手機 App 控制本機 session(本機 Claude Code 2.1.272 的說明)。

## 下一步

1. **層 1:誤偵過濾**(依決策表,要先寫預先登記)—— 新基準明訂為 `rf_cbiou` + 門檻 0.10,保留 `bytetrack` 0.30 回歸格;本輪不直接改 `configs/tracker.yaml`
2. 拓撲漏洞(525 次、佔可歸因 24.99%)已是 L2 最大單一成因,排在層 1 之後

---

# 續三:M4 做好了,M5 為什麼仍綁不對 —— 實測綁定規則

完整內容在 `docs/M5_綁定規則實測_20260915.md`(探索性調查,非預先登記)。

**使用者的要求**:「如實好好調查,不要亂自以為覺得」。

## 結論

**M5 決定「綁給誰」的分數,在目前設定下不含任何「這是誰」的資訊。**實際規則只剩兩條:

1. 候選掛在重疊鏡頭上 → 分數 `3.806 − ln k`(k = 那台重疊鏡頭上掛著的身份條目數)→ **k ≤ 8 必綁** → 綁給 k 最小者,同分任選
2. 其他 → **結構上無法綁定** → 開新身份

## 證據(`m5_cbiou`)

- **成功綁定 2,286 次,全部來自常數分數**;第一名是非常數分數的判斷最高 −0.815,0 次綁定(`m5_base` 同樣全部)
- `embedder none` 的外觀項是 **−1.19 的負證據**(不是中性)
- 轉場路徑理論上限:有連結最高 1.484、無連結 −2.41,**都低於門檻 1.609**;
  同鏡頭在 Δt ≈ 0 可過,但離場時間取自跟丟當下、廚師要 removed 才進候選池 → Δt ≥ 5 秒 → 最高 −0.73
- 正確的人在候選裡卻輸了 242 次:95 次同分、102 次輸在他那台鏡頭 k 較大、45 次他只能走轉場路徑
- 59 人第一次出現,27 人立刻被綁到別人身上;認對的 962 次也全靠常數分數,156 次是同分碰巧
- 誤併源頭:L3 40.6% / L2 33.6% / L0 25.3%;57.1% 的源頭判斷當下與第二名同分

## 調查中自己犯的錯(已修正,照實記錄)

| 錯誤 | 原因 | 修正 |
|---|---|---|
| 報「沒有任何候選 1,162 次」,實為 66 次 | 誤偵決策沒有 `n_cands` 診斷欄位,被讀成 0;與「成功綁定 2,286 次」自相矛盾才發現 | 改用 runner 對每筆都寫的 `n_candidates` / `score` |
| 報「非常數分數最高 0.762」 | 其實是 k = 21 的常數分數;k 上限設 20 的前提(單台最多約 9 人)錯了,k 是身份條目數不是人數 | 上限 60、改在分數空間比對 |
| 早期「是否為常數分數」分類不穩(換精確常數時 18 次判斷翻轉) | 在 k 空間用 ±0.01 比對,k 大時窗太寬 | 分數空間 ±0.0001(假命中率未另外量測) |
| 用「對方此刻是否在畫面」當候選路徑 | 重疊路徑也包含跟丟未移除的 track | 改名「對方此刻可見性」,註明不等於路徑,不據以下結論 |

另外更正 9/13 根因分解 §8.1 的「誤偵污染 embedding」:`embedder none` 下外觀向量恆為零,不成立。

## 新增工具

- `scripts/diag_m5_binding_rules.py`:結果×層、誤併源頭、候選組成(含一致性檢查)、L3 / L0 / 認對時的組成
- `scripts/diag_m5_score_ceiling.py`:各路徑分數上限、同鏡頭 Δt 表

## 下一步

步驟 3 決策表指向層 1(誤偵過濾),但本調查證明:**M4 或偵測的改善都不會改變綁定規則本身**。
要讓 M5 依「是誰」做選擇,需要在重疊路徑上以能區分身份、能拒絕的證據取代常數 `overlap_llr`。**下一步由使用者決定。**

---

# 續四:使用者決定先修 M4 —— 誤偵 track 組成診斷

完整內容在 `docs/M4_誤偵組成診斷_20260915.md`(探索性,執行前已向使用者說明分類規則與假說)。

## 使用者的決定

「我覺得我們要先把 M4 修好才能推進下一步」。
幾何量測的預先登記草稿 `docs/M5_幾何鑑別力_預先登記_20260915.md` **保留、未提交**,等 M4 修完再決定。

## 結果(`cbiou`,誤偵 1,113 條)

| 類別 | 條數 | 佔比 |
|---|---|---|
| ① 幾乎不碰到人 | 617 | 55.4% |
| ② 貼著人但框不準 | 275 | 24.7% |
| ③ 部分配對 | 168 | 15.1% |
| ④ 重複框 | 53 | 4.8% |

- ① 有 80.2% 在 camera_5、camera_6;**看影像與熱區圖:主要是玻璃上的人影倒影**(camera_5 另有走廊盡頭一個熱區未查明)
- ① 只有 43% 靜止 → **9/3 寫死的「靜止物件過濾」不對症**
- 信心分數:真人 track 25 百分位 0.680;四類誤偵 75 百分位都 ≤ 0.557。但 ②④ 是真人,門檻會一起砍
- 混人 389 條:第一次換人時兩人都有標註的 221 條中,**95% 兩人標註框互相重疊** → 交錯遮擋時換人
- 假說:H1 不成立(不是靜止)、H2 成立、H3 成立

## 過程中的事

- 系統 Python 沒有 cv2,影像用專案的 `.venv/Scripts/python.exe`
- 讀影片改用 `common.video_io.iter_frames` 逐幀讀到目標幀,不用 seek(AVI seek 可能不準)

## 下一步

寫修 M4 的預先登記。誤偵(倒影)與混人(交錯遮擋)分開;召回列為硬性約束。

---

# 續五:排除 camera_5、camera_6 重做 M4 評估

完整內容在 `docs/M4_誤偵組成診斷_20260915.md` §8。

## 使用者的決定與問答

- 「把有玻璃的資料集先撇除使用,再重新做一次」→ 看 7 台熱區後,選**排除 camera_5、6**(camera_2、4、7 也有玻璃或窗但誤偵很少)
- 問「為什麼要訓練模型」→ 確認**不需要訓練**,只重做 M4 評估(M4 追蹤器是規則,沒有模型)
- 問「之前為什麼要跑 6 小時」→ 9/12 修復網格 49 次完整 M3→M4→M5、當時沒有偵測快取(9/14 才建);現在 M4 評估每格約 20 秒

## 結果

| 指標(5 台) | `base` | `cbiou` |
|---|---|---|
| track | 2,413 | 2,070 |
| 誤偵率 | 21.6% | 24.4% |
| 召回(全部幀) | 88.4% | 92.5% |
| B 佔比 | 32.6% | 22.3% |
| 混人 | 298 | 297 |
| 誤偵組成 ①/②/③/④ | 23.6 / **47.0** / 20.7 / 8.6% | 24.1 / **41.9** / 23.9 / 10.1% |

- 硬性檢查全部通過:5 台每台指標、`tracks.csv` 逐列都與 7 台結果中的同一台相同
- **倒影拿掉後,誤偵最大宗是 ②「框在真人身上但不準」**;① 剩 122 條且 64% 靜止
- 混人:第一次換人時兩人框互相重疊 `cbiou` 94.6%、`base` 86.4%

## 遇到的問題

| 症狀 | 原因 | 解法 |
|---|---|---|
| `eval_m4_chirla.py` 報 `No module named 'yaml'` | 系統 Python 沒裝;專案環境在 `.venv` | 改用 `.venv/Scripts/python.exe` |
| `diag_m4_ghosts.py` 在 `base` 報 `could not convert string to float: ''` | bytetrack 匯出有空白 conf(5 台 1,304 列、7 台 3,540 列;`cbiou` 0),原因未查 | 空白讀成 None、不算進平均;修改前後 `cbiou` 5 台與 7 台結果都逐項相同 |
| H2 在 5 台資料印「不成立」 | camera_5、6 不在資料中,被當成否證 | 改印「不適用」 |

## 下一步

以 5 台 `cbiou` 為新起點,寫修 M4 的預先登記。主要對象:② 框在真人身上但不準、混人(交錯遮擋)。召回為硬性約束。

---

# 續六:拓撲的重疊清單與實際空間不一致(使用者提醒)

使用者問:「還記得我說只有兩台攝像機會照到同個房間嗎?」

- 查紀錄:9/3 使用者看房間總覽後說「cam2 cam3 好像照到同一個房間」,資料驗證成立;
  `docs/CHIRLA_鏡頭佈局實測_20260903.md` §3.3 的結論也是**只有 cam2 + cam3 同一個房間**
- 但拓撲 `overlapping` 有 **7 對**,來自 `chirla_build_topology.py` 第 157 行的機械規則(共現 ≥ 500、≥ 60% 兩邊框 ≥ 50×120),
  其中 5 對(2+4、2+5、3+4、3+5、4+5)小框寬中位只有 57~71 px,是不同空間遠遠看到
- **更正我上一則的說法**:「排除 5、6 後剩下的重疊有 2+3、2+4、3+4」是照設定檔抄的;依實際空間只剩 **camera_2 + 3**
- M4 不受影響;M5 的「k ≤ 8 必綁」會在這 7 對上觸發
- ⚠ 未提交的幾何量測草稿 `docs/M5_幾何鑑別力_預先登記_20260915.md` §1.3 也是用這份 7 對清單,回到 M5 時要一併重看

**處置(使用者同意)**:先修 M4;回到 M5 時再決定重疊清單是否只留 camera_2 + 3。已追記在佈局實測文件 §7。

另:`.gitignore` 加入 `results/m4_5cam/*_dump/`(5 台 track 匯出,約 18 MB,與 `results/m5_step3/**/m4dump/` 同樣處理)。

---

# 續七:②「框在真人身上但不準」為什麼不準

完整內容在 `docs/M4_誤偵組成診斷_20260915.md` §9(探索性,執行前已說明規則與假說)。

| 關係(5 台 `cbiou`,212 條) | 條數 | 佔比 |
|---|---|---|
| 偏小:上半身 | 78 | 36.8% |
| 偏小:中段 / 下半身 | 27 / 9 | 12.7% / 4.2% |
| 偏大:包住人 | 10 | 4.7% |
| 位置偏移 | 88 | 41.5%(一半跨到另一個人) |

- **遮擋**:58.0% 的 track;幀層級 71.7% vs 真人 track 49.2%(對照)
- 假說 H1(大多被遮擋)、H2(偏小多於偏大)都成立
- 8 條隨機樣本:被擋住時**標註框涵蓋被擋部位、偵測器只框看得到的部分**(3 條看到,未查證是否為標註慣例)
- **沒量的關鍵問題**:② 出現時同一個人是否已有另一條 track 在追(重複 track vs 被擋時唯一的 track,修法不同)

新增:`scripts/diag_m4_boxes.py`、`results/m4_5cam/viz_boxes.py`、`results/m4_5cam/boxes_cbiou.json`。

---

# 續八:② 大多是「同一個人被多開一條 track」;更正標註慣例的觀察

完整內容在 `docs/M4_誤偵組成診斷_20260915.md` §10(探索性,執行前已說明規則與假說)。

- 212 條 ② 中 **182 條(85.8%)是重複**:同一台鏡頭同一幀,已有另一條 track 配到同一個人;101 條是每一幀都如此
- **出生時刻就已有人在追:158 條(74.5%)** → 可在新 track 出生時處理
- 位置偏移 95.5% 是重複、上半身 83.3% 是重複
- 唯一的 30 條:一半在 ② 結束後 5 秒內原本的 track 重新開始(較像遮擋斷點)

## ⚠ 更正(續七寫錯的)

續七寫「被擋住時標註框涵蓋被擋部位」,只根據 3 條樣本。這次隨機抽 10 個被前面的人蓋住 ≥ 30% 的標註框看影像:
**能判斷的 5 個都是只框看得到的部分**。該觀察不成立,② 的 IoU 偏低不能歸因於標註慣例。

新增:`scripts/diag_m4_dup.py`、`results/m4_5cam/viz_occluded_gt.py`、`results/m4_5cam/dup_cbiou.json`。

## 下一步

寫修 M4 的預先登記。主要對象:**新 track 出生時,新框落在已被追蹤的人身上**(② 重複 + ④ 重複框),以及混人。
實際系統沒有真值,要先量「只用框的幾何關係能分到多準」。

---

# 續九:只用出生時刻的框幾何,分不開重複 track 與真的新人

完整內容在 `docs/M4_誤偵組成診斷_20260915.md` §11(探索性,執行前已說明標記、特徵、目標與假說)。

- 5 台 `cbiou` 出生 2,070 次:D 重複 375、N 新人 1,445、G 不碰人 159、U 其他 91
- **目標(抓到重複 ≥ 70% 且誤殺新人 ≤ 5%):沒有任何規則達標;H1(包含比例優於重疊)不成立**
- 事後調整(執行前沒寫):N 裡 151 個已有同一人的舊 track,扣掉後分母 1,294,**仍沒有規則達標**;
  最接近:重疊 ≥ 0.4 抓到 58.1%、誤殺 3.9%
- 8 個被誤殺的真的新人:5 個是**站在前面那個人後方的另一個人**,3 個是 camera_3 較早的框過大(人加椅子)
- → 「同一個人的局部框」與「後方另一個人的局部框」幾何上一樣,只看框分不開

## 下一步(待使用者決定)

需要幾何以外的線索。觀察:重複 track 通常很短(② 中位 1.5~2 秒、真人 4.5 秒)—— 未驗證能不能用。

---

# 續十:三個方向一起測 —— 時間、擋下重疊出生、混人換人

完整內容在 `docs/M4_誤偵組成診斷_20260915.md` §12。使用者:「都測試,反正要找到原因就對了」。

| 測試 | 結果 | 假說 |
|---|---|---|
| A. 等 W 秒再決定 | 丟掉的重複與新人比例幾乎一樣(< 5 秒:49.3% vs 53.6%);加幾何後仍無規則達標 | HA1 不成立、HA2 成立但不達標 |
| B. 重疊 ≥ 0.4 先不開(離線近似) | 重複**整條不開只有 14.7%**,大多只是延後出生;損失真人幀 1.15% | HB 不成立 |
| C. 混人 534 次換人 | a 已看不到 42.5%、互換 26.4%、a 還在被接走 25.7%;**b 已有自己的 track 59.4%** | HC1、HC2 成立 |

- 時間分不開的原因:出生時重複的 375 次裡 112 條後來變成那個人的主 track(存活中位 9.83 秒)
- ⚠ **更正**:先前說「重複 track 通常很短」言過其實(② 中重複的 182 條中位 2.50 秒,但 25~75 百分位 0.50 ~ 9.17 秒)
- **綜合(推論)**:人與人框重疊時,追蹤器只有框的位置/大小/重疊/運動可用,這些分不出誰是誰;
  `rf_cbiou` 關聯完全不看外觀。單鏡頭短時間的外觀鑑別力**沒有量過**

新增:`scripts/diag_m4_birth_time.py`、`diag_m4_suppress_sim.py`、`diag_m4_switch.py` 與三份 json。
