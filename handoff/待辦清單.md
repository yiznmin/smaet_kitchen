# 待辦清單 — 第一階段(交期 2026-09-11)

> 做完就把 `[ ]` 改成 `[x]` 並填日期,commit 回來。兩邊都看這一份。

---

## 🔴 今天就要做(會卡後面的進度)

- [ ] **寄 MMPTRACK 申請信** —— 研究限定授權,要簽 T&C 寄回微軟,審核要時間。
      **信裡要特別註明需要 `MMP-Retrieval labels`**(那個要另外要求)。
      信件範本見 `Datasets and Model Training-Test for MTMCT.pdf` 的「MMPTRACK:申請取得」段。
      寄到 `iccv2021mmp@outlook.com`,要用機構 email。
- [ ] **寄 AI City 2026 Track 1 / PhysicalAI-SmartSpaces 申請** —— 第二階段(9/12 起)要用,
      需機構或非商業 email 註冊 + 審核 2~3 個工作天。**9/12 才申請絕對來不及。**
- [ ] **HuggingFace 帳號 + 同意 CHIRLA 條款**(gated dataset,不同意下載不了)

## 🟠 曾薪:資料

- [ ] 下載 CHIRLA 到**遠端設備**
      `huggingface-cli login` → `huggingface-cli download bdager/CHIRLA --repo-type dataset --local-dir chirla`
- [ ] ⚠ **順便下載 `annotations/`** —— 第一階段只需要 `benchmark/`,但**第三階段
      (9/25~9/30 整合測試)要驗追蹤就得用逐幀標註**。分兩次下載 10.9GB 很浪費。
- [ ] `videos/`(原始 AVI)第一階段用不到,可以先不下載

## 🟡 宜蓁:環境(不需要資料,現在就能做)

- [ ] 遠端 `git clone`
- [ ] 建 Python 3.11 環境
- [ ] 依 `nvidia-smi` 的 CUDA 版本裝 torch(**不要直接 pip install torch**)
- [ ] `pip install -r requirements.txt -r requirements-reid.txt`
- [ ] `python scripts/check_reid_env.py` → **0 個 ❌**
- [ ] 記錄 GPU 型號、VRAM、磁碟餘裕 → 寫進紀錄
- [ ] `python scripts/verify_reid_metrics.py` → ALL PASS(確認環境正常,不需資料)

## 🟢 待寫的程式(本機做,不需要資料)

- [ ] **HDF5 embedding 匯出腳本** —— CHIRLA 官方 `evaluate_reid.py` 的輸入格式:
      `embeddings [N,D] float32` / `ids [N] int32`(負數=distractor)/ `paths [N]` UTF-8,
      放在 `embeddings/{method}/{model}/{scenario}_{split}_embeddings.h5`。
      沒有它就無法跟論文數字對照。
- [ ] **預先登記文件** `docs/CHIRLA_預先登記_<日期>.md` —— 訓練前把兩臂設定、
      評估協定、判準、可被否證的預測寫死並 commit。跑完只追加結果。

## 🔵 資料到位後(依序)

- [ ] `chirla_prep.py --verify` → **exit 0**(不過就不要往下)
- [ ] `chirla_prep.py --index`
- [ ] `train_reid_chirla.py --smoke` 確認能跑
- [ ] **S 臂**訓練(ImageNet 起點,可出貨)
- [ ] **R 臂**訓練(外部 Re-ID 權重,研究上限對照)—— 除起始權重外參數必須逐字相同
- [ ] 最終評估:只用 `_gallery` vs `_query`(開發階段只能用 `_train`/`_val`)
- [ ] 外部驗證:MMPTRACK(若已到貨)或 EPFL 6 身份站位
- [ ] **重新校準 `AppearanceLR.MEASURED`** ← 最容易漏掉
- [ ] 接回 M5:`sim_m5_montecarlo.py` 重跑,對照舊的 DINOv2 數字,
      看碎裂率與誤併率有沒有變 —— 這直接回答「這個模型對整套系統值多少」

## ⚪ 報告(9/10~9/11)

- [ ] 兩臂數字並列,**標明授權**(哪個能出貨、哪個不能)
- [ ] **先講基線水準**:CHIRLA 論文自己最佳的 ResNet101 也只有 CMC@1 18.81%,
      不要讓工研院看到 20% 以為模型壞了
- [ ] **「這次不能回答什麼」段落,放在結論之前**。已知至少三條:
      22 身份不足以證明泛化(PDF 自己也這樣說)、辦公室非廚房有域差、
      MMPTRACK 未到貨時外部驗證只有 EPFL 站位
- [ ] 過擬合的 train/val 曲線如實附上

---

## 時程

| 日期 | 事項 |
|---|---|
| 9/1 | 三封申請信 + 遠端環境 |
| 9/2 | 資料驗收 |
| 9/3 | 建索引 + **預先登記提交 git** |
| 9/4–9/6 | S 臂訓練與調參 |
| 9/6–9/7 | R 臂 |
| 9/8 | 最終評估(gallery vs query) |
| 9/9 | 外部驗證 + 接回 M5 |
| 9/10 | 報告彙整 |
| **9/11** | **向工研院報告** |

⚠ **緩衝很薄。** 若 9/6 前 S 臂還沒收斂,**砍掉 R 臂**(它只是對照),
優先確保主線與一份誠實的報告。

---

## 第二階段預告(9/12~9/24,方案 B:人物與物件 Re-ID 並重)

加入 NVIDIA PhysicalAI-SmartSpaces / AI City 2026 Track 1 的合成資料
(有 person + 服務機器人 + 堆高機 + 棧板車的跨鏡頭 persistent object ID),
再用 CHIRLA+MMPTRACK 對 person branch 做真實微調,並自建小型實拍物件測試集。

⚠ 該資料集以倉儲 layout 為主,與廚房有域差。
⚠ 下載只取 RGB + calibration + top-down + 2D/3D 標註(約 50GB),
  **深度資料可達 3TB,不要下載**。
⚠ PDF 明講「不可將 person 與 object 的分數合併成單一平均」——
  大量容易的人物樣本會掩蓋物件 Re-ID 的失敗。
